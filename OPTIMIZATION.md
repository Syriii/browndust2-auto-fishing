# 优化记录

按具体问题分轮推进，记录证据、验证与发布状态。待办不是已确认的故障，不依据猜测调整识别阈值。

## 2026-09-08：关联按键决策与失败证据

继续完善通用诊断，特殊机制等待后续实际样本。原失败包只有独立观察器画面，不能确保看到程序决定按键时的内容。现在控制线程复用当前 DXcam BGR 帧；仅按键尝试时复制区域，并记录策略、触发分支、坐标、时间、容差及相应挡板范围或阈值。按键决策写入 DEBUG 日志，并随逐次归属进入整轮结构化记录，不冒充游戏已收到输入或命中结果。

未命中/未确认包在原有最多 8 张观察帧之外，另附最多一张决策原图与三色原始掩膜，明确区分来源与坐标。PNG 编码和写盘仍在原有有界后台队列；命中明确后释放决策图，仅保留元数据。无对应按键的 FAIL 不误用上一张图；反馈无图但有决策图时仍保存，停止只消费已缓存图。不增加相机或截图调用，不调整识别阈值、按键条件、驱动暂停和反馈归属算法；按键前存在新增的区域复制与日志开销，尚未新增实机时序测量。

验证：119 项离线回归通过。新增检查覆盖原有黄区、蓝区回退、无指针、破冰及不按键分支在开关前后的输入行为；确认连续尝试不串图、无按键 FAIL 无决策图、停止且观察无图仍有证据、成功释放图片、底层帧复用后 PNG 不被覆盖，并逐像素核对决策图的三色掩膜。没有发送游戏输入；特殊机制识别与实机时序效果留待后续实践，不以离线回归宣称已经验证。仅合并 Python 源码，不构建或更新 EXE。

## 2026-09-08：获取社区攻略与机制参考

通过浏览器读取巴哈姆特《你我都能當個海王 - 釣魚全攻略 3.0》主楼正文，核对用户提供的 13 种技能，整理到 `docs/FISHING_MECHANICS_REFERENCE.md`。补充与日志有关的蓝区缩完产生 FAIL、绿色维持区可能无黄条、技能感叹号与上钩感叹号不同、技能可叠加且隐形指针不提示等规则说明。社区数值未逐项实测，不作为已验证常量；鱼表仅作为查找现场证据的线索。

核对当前策略确实没有绿色长按流程或真假指针专门跟踪；破冰仍只是原有尝试按键，挡板策略不代表覆盖全部技能。README 修正无对应按键 FAIL 的原因说明并链接参考文档。本次仅文档修改，核对源文与代码并检查差异；不运行游戏、不改变控制与留图逻辑，不以文档核对冒充新实机测试。

## 2026-09-08：日志语义、级别和轮次关联

检查 14:06 的一分钟实测日志：排除 481 条逐帧 DEBUG 后仍有 171 条 INFO/WARNING，其中 34 条 WARNING 只是按键归属未知，2 条是游戏 MISS；另有游戏反馈与按键归属重复汇总、“破冰成功”未经验证、正常失焦停止附带堆栈等问题。

统一默认展示：每个游戏反馈一条中文 INFO，每轮一条结算汇总，暴击/普通命中/未命中均使用实际反馈口径；按键尝试与归属未确认另列。逐次按键结果、匹配依据、控制采样、单次截图路径与证据 ID 保留在 DEBUG 文件日志，未知或游戏失误不再逐条刷 WARNING。预期停止在观察器中只写 DEBUG 原因，真正采集异常保留 ERROR 与堆栈。原来的“破冰成功”改为“已发送破冰尝试按键；效果尚未核实”。

增加 `logging_context.py`：从每轮抛竿开始分配独立 ID，同步流程使用上下文，后台反馈对象与保存任务绑定原轮次，避免下一轮开始后串号；OCR 原始警告仍保留等级、原文与位置。QTE、上钩超时和整轮 ZIP 增加 round_id/evidence_id；循环覆盖的槽位仍保持限量，分析旧日志时通过 ID 检查是否已经覆盖。

文件处理器改为识别项目自己的实例，其他库已安装 handler 时仍建立滚动文件，不重复创建；原生崩溃文件重启追加。Python 录制工具同样保留 DEBUG 到文件、INFO 到控制台。没有修改按键条件、识别阈值、反馈归属算法或截图内容。

验证：114 项离线回归通过，包括两轮主流程的编号传递与中断清理、后台保存与下一轮隔离、覆盖槽位 ID 区分、文件完整保留逐次记录、预期停止与真实异常区分、已有 handler 下文件初始化。真实 Tk 模拟检查验证默认/详细/警告筛选和两次启停。14:06 两轮实测元数据通过当前记录逻辑离线回放，普通输出保留全部 22 次游戏反馈，文件保留全部 49 次按键归属，没有伪 WARNING；普通命中仍为 1。回放输出在工作树 `debug/logging_review_after/normal.log`、`complete.log`。本轮未新开游戏实测，日志回放不冒充重新验证命中率；仅交付 Python 源码，EXE 不更新。

## 2026-09-08 14:05 起：合并版本补充实机验证

用户解锁并确认就绪后，从主目录 `efc53d8` 启动 Python 限时实测，真实游戏客户区仍为 875×492，自动清包与自动换场在测试进程内关闭；不启动 EXE，不更改根目录配置。

14:05 首轮沿用一次抛竿受阻恢复，正常检测上钩。实际记录 11 次 QTE 按键（5 暴击、1 MISS、5 未确认），另有 5 次无对应按键的 FAIL。整条鱼退出时计时器多次为 1、剩余距离 3650cm、没有奖励，记录疑似超时逃脱。随后失焦停止，恢复电源请求并释放输入。逐一检查 10 份 QTE 诊断包的 80 张原图及 240 张同帧颜色掩膜；全部一致。`verified_miss.png` 目视可见 MISS 和当时色条。全部 11 次实际 QTE 调用各有且仅有一条按键结果。

14:06 的 60 秒会话第一条鱼记录 8 次 CRITICAL、1 次 HIT、2 次 MISS；27 次按键结果完整。HIT 的两次候选按键仍为未确认，但游戏反馈汇总没有丢失普通命中。结算为疑似超时逃脱，剩余距离 1035cm。第二条鱼在 QTE 期间由 60 秒时限停止，22 次按键结果及最后 ROI 均落盘，状态为 interrupted，没有误报逃脱；停止请求后没有新的 press 调用。测试停止文件的创建晚于时限，因此本次验证的是定时取消路径，不能称为停止文件触发成功。

14:08 的最后一轮首条鱼 QTE 已退出但缺少完整结算证据，保守记录 unknown，并保存全图与计时依据；下一条鱼在 60 秒时限中断，29 次按键结果及已有 ROI 正常保存。此次补测没有新增确认捕获样本，捕获正例仍依据此前 13:03 的实测与真实截图回归。

三次会话共 105 次真实 QTE 按键均有且仅有一份逐次结果，5 份整轮记录全部带图（2 suspected_escape、1 unknown、2 interrupted）。逐一核对 30 份 QTE ZIP，240 张原图与 720 张白/黄/蓝掩膜完全一致，所有 ZIP 均完整；三次记录中停止后没有新的按键调用。最后只读检查 space、up、t 与鼠标左键均已释放。未发现本次结果记录功能的新回归，不修改核心策略。

证据保存在主目录源码 `debug/qte_live_20260908_140514/`、`debug/qte_live_20260908_140653/`、`debug/qte_live_20260908_140837/`，各自保留轮转 ZIP 的快照；核对汇总为 `debug/merged_live_audit_20260908.json`。这里的“通过”指结果观察、证据保存及停止保护；实际仍有鱼未捕获，不能据此宣称当前策略能应对全部特殊机制或达到百分之百命中。源码已在 main，实测后仅追加本记录；EXE 与根目录用户配置未更新。

## 2026-09-08：完成 QTE 结果记录的退出路径和页面设置

当前源码默认开启 QTE 反馈及鱼获结算观察；页面新增“记录 QTE 结果、失败截图和鱼获结算”，开始时保存和应用，运行期间禁止修改。已有配置明确关闭时沿用其值。原光标选择、黄蓝条阈值、按键条件和驱动暂停参数不变。下方“默认关闭/未合并”描述为此前各轮的历史状态。

新增整轮收尾入口 `run_observed_qte`，正常退出、控制超时、异常与停止均先关闭反馈观察器，再保存完整逐次结果，避免最后一笔待确认按键从结算 JSON 中遗漏。没有按键或识别不到倒计时仍保留已有 ROI；OCR 中途失败仍保留已经取得的结算图。停止后只处理缓存，没有新的截图、OCR 或游戏输入。没有可用图片明确写入元数据；队列满、保存失败或超过有界等待时给出日志。未知不等于 MISS，疑似逃脱仍是推断；特殊机制补全和非基础内容自动检测仍按约定暂缓。

验证：108 项离线回归通过，覆盖真实反馈与结算图、原策略按键行为、所有退出路径、完整末次结果、OCR 失败与停止传播。Tk 集成检查通过两次启停、开关关闭/开启的配置保存和实际任务参数、运行时禁用、最小窗口布局；使用临时配置与模拟任务，没有游戏输入。实际 Tk 检查须在可访问 Tcl 的桌面权限下运行，沙箱内的 Tcl 查找失败不作为 UI 回归结论。

对 13:03 两份捕获与 13:11 疑似逃脱的实机结算原图、计时记录重新执行真实 OCR 和当前写盘路径，结果保持 caught/caught/suspected_escape，分别完整保留 8/8/7 条游戏反馈。核对输出在工作树 `auto_fishing-dev/debug/completion_replay/`。既有真实游戏验证见下方记录。

13:32 新的 60 秒 Python 实测在桌面可交互检查处被拦截，发生在聚焦和发送输入之前，清理已执行；不能写成新增实测通过。此次最后的退出路径改动由离线回归验证，界面由真实 Tk 模拟验证。发布范围仅 Python 源码合并，不构建或覆盖 EXE，不迁移根目录用户配置。其他窗口尺寸、语言与新增特殊机制尚未实机验证。

## 2026-09-08：游戏反馈与按键归属分开统计，新增整条鱼结算观察

继续在 `test/qte-feedback` 工作树完善结果观察。此前“已看到 HIT/CRITICAL，但连续按键无法归属”只计入按键未知，导致游戏已明确给出的结果从汇总中遗漏。现在每个去重后的游戏反馈独立记录结果、时刻及候选按键；按键归属另行统计。在反馈出现时重新核对原有 750 ms 候选窗口，排除已过期的旧按键，保留仍无法归属的尝试为未知；没有调整实际按键条件或驱动暂停参数。

新增 `catch_result.py`。QTE 正常结束、原关闭点击之前截图并检查结算关闭提示、奖励数量及尺寸，证据充分时报告“确认捕获”。生僻鱼名可能拉低整行 OCR 分数，增加放大文本框和独立数量复核，保留 OCR 读数而不编造鱼名。截图与 OCR 使用原结算等待预算，耗时超过预算时可能延后关闭；停止检查可阻止之后的点击。独立 GDI 观察线程每 250 ms 尝试读取计时器，与结算 OCR 共用锁，避免并发调用同一引擎。

未捕获没有稳定的明确文字样本。当前仅在近期多次计时器读数接近耗尽、距离仍大于零、QTE 退出且没有奖励证据时报告“疑似超时逃脱”，不冒充游戏明确宣告失败。末帧数字被特效遮挡时最多回看三张近期 QTE 图，并分别记录计时器和距离的时间、原图。看不到奖励本身不构成逃脱证据；中断单独标记。低级特殊机制补全和非基础内容检测继续暂缓。

整条鱼结算证据包位于 `debug/catch_result/`，包含结算全图、最后计时器图、距离依据图（若使用）、OCR 原始依据、游戏反馈与按键归属。后台队列最多 2，按 max_events 循环保留。功能仍由默认关闭的 qte_feedback_enabled 控制。控制流程超时但尚未观察到结算时仍是未知，不假装已经捕获或逃脱。

验证：105 项完整离线回归通过。真实旧录像回放确认 14.7cm 奖励，并验证空场景不能判定逃脱；24.7cm 生僻名称样本验证数量独立复核。13:03 的 60 秒 Python 实测确认两条鱼（OCR 尺寸 28.6cm、21.7cm），每轮记录 8 次暴击反馈、15 次按键尝试，无法归属的暴击不再遗漏。13:05 探针出现 MISS/FAIL 后仍确认捕获，验证单次失误与整条鱼结果独立。13:11 的只观察探针实测记录疑似超时逃脱（剩余 1980cm），随后被时限中断的下一轮标记 interrupted。

检查了两份捕获和一份疑似逃脱结算 ZIP、结构化记录与原图；最后的奖励排除检查用实机截图离线复核通过。四次完成的有界会话均无时限后的新按键，只观察探针的 QTE 阶段无按键。记录在工作树 `debug/qte_live_20260908_130333/`、`130522/`、`130749/`、`131150/`（后三个目录同样带 qte_live_20260908_ 前缀），核对汇总在 `debug/catch_inspection/final_audit.json`。未合并、未构建或更新 EXE；逐个按键仍不保证明确归属，逃脱仍是保守画面推断，其他窗口尺寸未实机验证。

## 2026-09-08 12:26–12:34：工作树实机反馈验证与截图冲突修复

从 `test/qte-feedback` 工作树运行 Python，12:26 首轮进入 QTE 后，反馈线程创建第二个 DXcam 会话返回 `E_INVALIDARG`（参数错误），无法留图；该轮判定测试失败。中断该诊断进程后补充调用受控输入释放。本轮只修复诊断采集：新增 GDI 小 ROI 采集 `feedback_capture.py`，沿用原控制线程的 DXcam、光标选择、黄蓝条阈值和按键时机。独立观察覆盖原 press 约 300 ms 的等待期间，避免漏掉反馈；不新增特殊机制策略。

只读采集检查通过：GDI 与 DXcam 各产生 30 帧，目视核对两种截图内容与 BGR 颜色一致；连续三次创建/关闭后 GDI 对象数均恢复为 0（采集期间为 3）。350×143 区域单次 GDI 截图中位 8.85 ms、P95 11.96 ms，不包含匹配和编码，不代表其他分辨率开销。记录在工作树 `debug/feedback_capture_20260908_123015/`。

12:30 普通策略实测 45 秒，完整退出一轮 QTE：27 次按键尝试、11 次确认暴击、16 次未确认，其中包含连续按键和动画残留造成的归属不清。它们不是 16 次失败，也不能据此计算真实命中率或捕获成功率。12:32 另一次 45 秒探针实测识别到 MISS、HIT、FAIL、CRITICAL；探针的色条目标只是采样意图，最终结果仍以游戏文字为准。无待确认按键的 FAIL 保留为独立事件。探针观察器独立关闭后再启动原策略，避免嵌套创建资源。

两次修复后的会话到时均正常退出、恢复电源请求并释放按键；时序文件核对 45 秒时限之后没有新的按键调用。保存包使用原有 10 个槽位循环覆盖，测试结束另存快照供复核。20 个 ZIP 的完整性、160 张原图和 480 张同帧颜色掩膜逐一核对通过，失败画面 `debug/qte_live_20260908_123252/verified_fail.png` 可见清晰 FAIL 与当时 QTE 条。全套 93 项离线回归通过，新增 GDI 初始化失败/截图失败资源释放、BGR 独立拷贝测试。

当前通过范围：本机 875×492 窗口的反馈识别、失败/未知留图、正常 QTE 退出和限时停止。逐次按键归属仍允许“未确认”，特殊机制应对与整条鱼捕获结果未新增判定；其他显示模式仍未验证。工作树功能仍默认关闭，尚未合并或发布 EXE。以下较早记录中“双截图实测未完成”的限制已由本次 GDI 与 DXcam 并行检查及实测更新。

## 2026-09-08：独立工作树测试（未合并）

原项目没有 Git 历史，先将当前源码（包括此前实验性 QTE 反馈功能）保存为本地 `main` 基线 `f23a8f1`，再建立 `test/qte-feedback` 分支。测试目录为 `E:\entertainment\BD2_AutoFishing\.worktrees\qte-feedback-test`。发布程序、虚拟环境、运行日志和诊断数据未纳入版本管理。该基线不是修改前的历史版本。

工作树内修复调试工具对父目录部署配置的强依赖：新增只读 `--config` 参数，未显式指定且没有部署配置时读取本工作树的源码配置。沿用原目录 Python 虚拟环境执行工作树源码，未复制或升级依赖。

验证：工作树完整离线回归 90 项通过，包括 3 项配置选择与只读行为测试。11:41 使用工作树 Python 源码进行 45 秒实测，明确启用反馈观察器、关闭高频全窗口录像与自动清包，只读原目录用户配置。Windows 桌面可交互检查在游戏输入前失败，未进入钓鱼流程，退出清理已执行。日志与空输入时序在工作树 `auto_fishing-dev/debug/qte_live_20260908_114116/`。

合并条件仍未满足：桌面恢复可交互后，继续验证新增观察器与原相机并行、每次 QTE 结果归属、失败留图及停止退出。当前不将离线通过等同于实机通过，不合并测试分支、不发布 EXE。

## 当前范围与暂缓事项（2026-09-08）

本项目优先服务用户当前的高等级角色，先完善已有策略的单次 QTE 反馈判定、失败/未确认留图及停止保护。低级 QTE 的旧特殊机制暂不补全，不因缺少这些机制的支持而扩大当前核心按键策略。

挡板、贝壳、真假指针等机制后续按真实失败样本逐项补充：保留原图、同帧掩膜和反馈时序，确认影响后再建立回归样本、修改对应策略。“QTE 条出现非基础内容就记录”只是用户提出的待讨论方案；当前不实现额外内容检测，也不把它作为已确定的自动留图条件。

## 2026-09-08：单次 QTE 反馈与失败留图（实验性源码功能，默认关闭）

本轮补齐无新帧时的反馈超时结算，避免将采集停顿当作文字消失而重复计数；证据抽样优先保留反馈出现帧和按键前后帧，增加相对时间元数据。全套 87 项离线测试通过，其中 QTE 反馈 19 项。更新后对第一轮真实录制回放仍为 13 次按键、6 次确认暴击和 7 次未确认，每次按键恰有一条结果；不能把未确认等同失败。

小区域留图离线测量：875×492 客户区、350×143 的证据 ROI、每包最多 8 帧及三色掩膜，7 包各约 0.60–0.65 MiB；编码写盘中位约 31.6 ms、P95 约 46.5 ms，在后台进行。按默认 10 个槽位估算同类画面约占 6–7 MiB，不代表其他分辨率上限或双相机实机性能。复现脚本与汇总在 `debug/verify_qte_feedback.py`、`debug/qte_feedback_verification_20260908/`。

11:35 使用 Python 运行 `tools/record_qte_session.py --seconds 45 --feedback --no-full-frames`，正常采集路径的有界实测在开始前被“当前桌面不可交互”保护拦截，未进入钓鱼或发送钓鱼输入，退出清理已执行。因此新增双相机观察器仍待桌面恢复可交互后验证；没有修改保护、默认开关或根目录发布程序。

用户说明黄条为暴击、蓝条为普通命中、MISS/FAIL 为未命中且增加倒计时损失；挡板、贝壳、真假指针等特殊机制需要失败现场来分析。按授权使用 Python 脚本采样，10:55 和 11:00 两次有界会话完整保存截图与按键时序；明确采到 CRITICAL、MISS。第三次采样被用户停止，从已完整写入的 ZIP 本地条目恢复到 HIT、FAIL 字形，原始时序元数据未完成写入，不能用恢复帧核实按键归属。未将 EXE 用作实测程序，根目录发布版未修改。

新增 `qte_feedback.py` 和字形资源。QTE 策略仅增加可选观察器生命周期及按键尝试通知，仍调用原 `controlled_input.press("space")`；未调整光标选择、黄蓝条颜色阈值、挡板/贝壳处理、驱动暂停参数或流程退出判据。观察器独立只读采集小 ROI，匹配新出现的 CRITICAL/HIT/MISS/FAIL；重复动画不重复计数，无反馈或多次输入归属不明为“未确认”，无对应输入的反馈单独记录。失败/未知保存有界前后图及同帧原始颜色掩膜、坐标和时间，后台编码，队列最多 2 份、按现有 max_events 控制磁盘槽位。观察帧不保证与按键决策帧相同，未加入自动特殊机制诊断或捕获成功判定。

源码与默认配置 `qte_feedback_enabled=false`，需 Python 调试显式启用；构建脚本已包含字形资源，但未构建或部署。新增观察器的双截图会话并行运行、焦点切换时原生资源退出、真实运行 CPU/磁盘开销以及其他分辨率/鱼种均未实机验证，不能将离线字形测试当作实机命中率。

离线首轮回放 13 次按键各有一条结果：6 次确认暴击、7 次未知，未将重复按键当成命中。仅匹配步骤的本机离线中位耗时约 3.6 ms、P95 约 4.6 ms，不含截图、线程调度或写盘。原始全窗口高频调试包约 229 MB/358 帧、PNG 编码及写入中位约 47 ms，仅说明全窗口采样成本，不能外推为小 ROI 留图成本。实际实图正例/负例、结果归属、旧动画去重、未知兜底、原输入调用和停止信号、证据 ZIP 已完成离线验证。

## 2026-09-08：真假指针反馈与记录能力核对（待定位具体轮次）

用户已移除装扮，另反馈一轮 QTE 出现一真三假指针、未能捕获且没有报错。核对当前进程 5552 来自根目录 EXE；当前可读日志最后为 10:33:16，尚未获知该轮发生时间，不能把先前某轮日志当作本次事件。

README 原有多光标处理记录是缩窄真实光标颜色范围。现实现以 HSV 饱和度 0–10、亮度 240–255 筛白色，再取白色像素最多的一列；没有多候选数量、真假身份或轨迹判别。若多个候选均通过颜色筛选，选择结果可能被假指针影响，但无该轮原图，尚未确认实际原因。

当前亚特兰蒂斯使用 `FrostStraitQTEStrategy`，日志只传单一 `cursor` 和是否按键；`yellow=None` 表示未记录黄色数量，不能解读为零。未识别黄条不会自动报错，倒计时条消失按 INFO 结束；现有诊断截图只覆盖等待上钩超时，不保存 QTE 多指针帧。即使开启现有逐帧日志，也不会增加候选列表或 QTE 截图。后续需要对应轮次及真实 QTE 样本才能区分颜色漏检和假指针干扰。本轮只核对并澄清文档，未调整识别逻辑、配置或发布版本。

## 2026-09-08：装扮遮挡与昼夜对比（分析完成，尚未修复或发布）

用户确认头顶云朵来自装扮，反馈白天通常正常、夜间漏检。对比亚特兰蒂斯同一 875×492 窗口、同一绝对 ROI 的两份诊断：`timeout_02` 白天峰值 127，`timeout_01` 夜间峰值 122，当前规则要求 `>127`，两份均未触发。白天 10:20:52 的日志也明确记录该次超时；不能将现象描述为白天从未漏检。

两张原图均存在遮挡。以 ROI 第 28 行仅作这两帧的解释性分界，上部感叹号可见区域在白天计入 106 像素，下部装扮区域另外贡献 21；夜间分别为 122 和 0。白天偏黄的云朵混入了检测计数，夜间蓝色云朵不再贡献；这支持“遮挡叠加装扮昼夜颜色变化导致计数脆弱”的解释，而非仅凭总像素数推断夜间感叹号变暗。两帧的动画位置不同，且均来自超时采样，不能从中测定遮挡比例或昼夜漏检率；此前成功触发只有日志，无同帧原图。

已保留 6 张含感叹号峰值及 22 张负例和来源元数据于 `tests/fixtures/hook_small_window/`。可复现对比脚本、图表和统计在 `debug/hook_inspection_20260908/`。原有等待流程与夜间色相回归共 10 项通过。撤回尚未实施的参考阈值 220→200 方案及依赖该方案的临时测试，生产源码、配置和根目录 EXE 均未调整。本轮未操控游戏；后续修复应验证装扮干扰与可见感叹号特征，不能只依赖白天云朵贡献的总像素数。

## 2026-09-08：截图输出缓存与实时枚举不一致（当前发布版本）

08:09:40、08:10:01 在 `DXFactory.create` 读取 `self.outputs[device_idx][output_idx]` 时越界；游戏客户区位于右侧显示器 `(1936, 577, 2811, 1069)`。旧适配层实时枚举出索引，但 `dxcam.create` 使用模块初始化时缓存的输出列表，显示器变化后两者可不同。日志没有保存当时旧列表，不能确定最初触发变化的是唤醒、热插拔还是其他显示状态变化。

`display_support.py` 现在保留同次枚举的适配器与原生输出对象，直接创建独立 `DXCamera`，不将新索引传给进程级旧工厂。每次截图会话重新枚举，记录输出列表与游戏区域，初始化期间布局变化明确提示；初始化后取消会释放尚未交给调用者的相机。仍使用固定 DXcam 0.3.0 后端，未升级依赖或改动游戏按键逻辑。

68 项离线测试通过，包含旧工厂列表过短、新输出增加/排序改变、会话重启、创建中取消及显示器消失。`tools/smoke_capture.py --simulate-stale-factory` 在正常桌面的独立进程故意清空旧工厂输出缓存，连续三次创建/释放相机，均成功从右侧 DISPLAY1 截取 875×492 BGR 游戏画面。该检查没有聚焦、移动窗口、发送输入或实际切换显示器；真实休眠/热插拔过程及整轮钓鱼未测试。

已完成图形版构建、EXE 预览待机启动和 ZIP 完整性验证。确认旧进程 30248 已停止任务并释放输入后关闭，根目录 EXE 与配套依赖已替换，1164 文件哈希一致。SHA256：`cbb24c63dd18bab4b697bc2a5c855a135665bc81f107ec83ae227c1d79b70c30`。备份：`debug/release_backups/20260908_081709/`。配置字节级保留，包括关闭自动清包、开启防自动睡眠；日志保留。

## 2026-09-07：地点识别有限重试

18:02:58 地点 OCR 在 ROI `(112, 616, 261, 650)` 仅返回“更改”和“N”，并非无图或引擎抛异常。该区域与此前成功识别深渊巨口时相同；旧逻辑仅采样一次，未匹配就结束启动。缺少当时原图，不能断言漏识别具体由画面变化还是 OCR 导致。

地点识别最多采样三次，失败后等待 0.5 秒重新截图，首次成功立即返回；日志逐次保留区域、识别文字和下一步。仍失败时保留页面手动选择提示，不推测钓场。取消或窗口保护可中断重试，手动选择及关闭自动识别时不进入重试。

64 项离线测试通过，包含原始部分文本、无新帧后恢复、持续失败上限、首次成功及取消。构建 ZIP 完整性通过，确认旧 EXE 已退出后完成根目录替换，1164 文件哈希一致。SHA256：`7473a1844ff9a3ecdfaf223dba516a4e58146daa643e4a15282f77ee2b549368`。备份：`debug/release_backups/20260907_180643/`，用户配置和日志保留。未实测本轮 OCR 重试在游戏中的恢复效果。

## 2026-09-07：恢复无法抛竿时的自动移动

用户指出原来已有强制移动。复核 `operate.recover_from_timeout` 确实会按住向上 2 秒、点击游戏画面并重抛；上一轮遇到明确失败直接停止，截断了原本等待超时后可执行的恢复路径。

改为 OCR 抛出明确的位置受阻信号，由上钩等待流程立即复用原恢复动作，不再等 15 秒超时。同一轮位置恢复一次后再次收到失败提示才停止；计数不因重抛重置，下一轮重新开始计数。恢复后重置等待时钟、峰值和诊断，继续检查抛竿提示。保持停止响应、窗口保护和自动清包开关语义，禁止绕过开关出售。

59 项离线测试通过，新增立即恢复、完整等待、恢复仍失败和恢复中取消回归。已构建并核对 ZIP 完整性。确认旧进程 30248 来自根目录且已停止释放输入后关闭；根目录 EXE 与配套依赖完成替换，1164 文件哈希一致。SHA256：`4391663019115156a049c40f41fa85f45a2fbac64304beac05b2b3b76e5d383e`。旧版本备份：`debug/release_backups/20260907_180114/`。配置与日志保留；未进行本次真实游戏移动或捕获测试。

## 2026-09-07：抛竿失败提示

实机日志显示：17:49:55 首轮在开始等待期间因游戏失焦而停止；17:50:06 第二轮发送抛竿按键，17:50:09 OCR 原文为“当前位置无法抛竿。请移至船边重新尝试。”，17:50:19 再次失焦停止。已成功聚焦、截图和识别钓场，不能据此认定输入完全未执行。原逻辑忽略非满包提示，且把按键发送记为“抛竿完成”，容易误导。

现在复用原抛竿后 OCR 结果识别明确的“当前位置无法抛竿”，保留原文 WARNING，停止任务并提示手动移至船边。支持原句、空白及分框拼接；不增加截图/OCR，不自动寻找钓鱼位置。按键日志改为“已发送抛竿按键，等待游戏响应”。失焦保护保持不变。

56 项离线测试通过，覆盖实机文本、分框文本、空结果及原满包行为。发布 ZIP 完整性通过，已更新根目录 EXE 和依赖并核对 1164 个文件哈希。SHA256：`f26875b7c35b14a551b50c80834a1d3ce7160453ab48eaf6447b69ebcd770243`。备份在 `debug/release_backups/20260907_175356/`；用户配置及日志保留。本轮未进行实际钓鱼测试，角色位置仍需用户调整后验证。

## 2026-09-07：仅通过页面按钮启停

按用户要求移除 F12 全局监听、快捷键提示、旧控制台快捷键入口与 `tools/smoke_f12.py`。控制器更名为 `TaskController`，开始和停止为独立操作；重复开始或停止不能意外切换状态，原生调用尚未返回时不能另开工作线程。保留失焦、锁屏、关闭页面时取消任务并释放输入的保护。以下历史记录中的 F12 和 `--console` 描述不再代表当前用法。

53 项离线测试通过（包括重复开始、停止期间防止重复工作线程、异常后重新启动），实际 Tk 两轮模拟按钮启停、日志筛选和最小布局检查通过。打包后的 EXE 预览待机启动与 ZIP 完整性验证通过；未发送真实游戏输入。

已成套覆盖根目录 EXE 与 `_internal/`，1164 个文件哈希与构建产物一致。EXE SHA256：`b0d6c69003a7490b2e626366f5c0483c29d11676613102987858a0cff92c91c4`。旧版本及配置备份在 `debug/release_backups/20260907_174752/`。用户配置与日志保留，自动清包仍关闭。

## 2026-09-07：修复重新抛竿后的等待窗口

**问题与证据**：`FishingBot.wait_for_bite` 原先在恢复动作或背包清理前设置等待起点。恢复动作中的移动、点击、抛竿，以及可能超过 15 秒的背包处理，都占用了下一次上钩等待时间。清包路径还保留了前一窗口的峰值与诊断帧。

**修改**：恢复或清包后的抛竿完成后，重新读取单调时钟，清零峰值并重置诊断。进入下一次循环后再检查背包提示，避免沿用动作执行前的时间。

**验证**：`tests/test_bite_wait.py` 使用模拟时钟和模拟游戏接口，覆盖恢复后完整等待、20 秒清包后正常等待、旧诊断数据清除。将旧计时逻辑仅在测试内回放，三个用例均失败；修复后三个用例均通过。完整离线测试共 21 项，全部通过。无需真实等待或出售游戏物品。

**发布状态**：本轮修改源码，未重新构建或覆盖根目录 EXE，未进行真实游戏测试。真实捕获成功率仍需实机观测，不能由离线用例推断。

## 后续候选事项

| 优先级 | 事项 | 当前证据与下一步 |
| --- | --- | --- |
| 中 | 真实环境的焦点与电源验证 | 已加入失焦/锁屏停止和可选电源请求，继续在用户方便时验证真实聚焦、混合 DPI 和唤醒后的重启；不自动锁屏或让用户电脑睡眠。 |

每轮仅处理有证据且能验证的范围，完成后更新这里的状态；按用户反馈调整优先级。

## 2026-09-07：背包开关与多显示器

**修改**：新增 `[backpack] auto_clear_enabled`。开启时按游戏满包提示清理，关闭时满包停止待机；不依赖固定鱼数。源码默认与旧配置兼容值为 `true`，当前根目录用户配置设置为 `false`。

截图不再固定主屏，按游戏客户区选择 DXGI 显卡/输出并转换为屏幕局部坐标。鼠标绝对移动改用虚拟桌面范围，支持负坐标和坐标 0。启用每显示器 DPI 感知；循环与输入前检查窗口位置、尺寸和最小化状态，变化后停止并提示 F12 重新定位。跨屏窗口明确拒绝，不拼接画面。

**验证**：完整离线测试 33 项通过，覆盖开关、负坐标、副屏、多显卡索引、窗口变化及释放输入。真实桌面只读检查枚举出两块 1920×1080 显示器，分别在 `(0,0)`、`(1920,0)`；两块屏幕的小区域均成功返回 BGR 图像，游戏客户区截图尺寸为 1132×636。未移动游戏窗口或发送鼠标点击；负坐标、多显卡及混合 DPI 尚未在对应硬件组合上实测。

**发布状态**：候选发布包已构建，ZIP 完整性及新增模块收录检查通过。EXE SHA256 为 `143b3726baa28e6dd5a0e2adcd58b0b4ae1fc46bb1c373004ed765d9cff51b3a`。用户要求稍后更新，故保留根目录旧 EXE 运行，不执行覆盖或 F12 实机启停测试；后续修复完成后需重新构建再部署。当前根目录配置中的新开关只有更新后的程序才会读取。

## 2026-09-07：保留诊断信息并减少重复日志

**证据**：RapidOCR 3.7.0 默认 `propagate=False`，警告只进自己的控制台处理器，没有进入项目文件。空白图片可复现 `The text detection result is empty` 并返回空列表。深渊巨口策略每帧 `print(blocker_rect)` 导致重复刷屏。

**修改**：RapidOCR 保留所有原始消息与等级，统一进入项目日志，并补充当前 OCR 场景及区域。业务层区分无新帧、预期可无文字、应有文字但为空、截图异常和执行异常。QTE 默认每 5 秒及退出时汇总，文件记录挡板每次位置变化；`[diagnostics] qte_detail_log=true` 可启用逐帧文件记录。记录倒计时消失、达到最长时限、取消和异常退出原因，不改变按键判断或宣称捕获成功。

**验证与发布**：完整离线测试 42 项通过，包含日志回归以及两个真实策略的离线模拟回放，检查消息留存、重复汇总、停止/异常补齐、按键与结束行为。本轮不重新打包或覆盖；此前候选 EXE 尚不含本轮日志改动，统一发布前必须重新构建。

## 2026-09-07：整体日志与桌面程序页面

**修改**：默认启动 Tk 图形页面，提供开始/停止、钓场选择、自动清包、保持唤醒、逐帧日志与日志筛选。F12 和按钮共用控制器；工作线程经有界队列投递日志，界面展示上限不影响文件留存。业务 `print` 改为日志记录，阶段状态独立显示，保留 `--console` 和不连接游戏的 `--preview`。

启动时恢复并聚焦游戏，确认成功才初始化；无控制台时 OCR 无法识别地点会提示从界面选择。常规任务也检查焦点和输入桌面，等待期间最多每 50 毫秒检查取消/窗口条件。失焦或锁屏后停止，不自动续跑。可选电源请求仅防止自动息屏/睡眠，默认关闭，结束时恢复原线程请求；不更改电源计划或绕过锁屏。

**验证**：53 项离线测试通过；实际 Tk 的两轮模拟开始/停止、日志筛选和最小窗口尺寸布局检查通过。窗口截图工具的读取授权超时，未完成截图视觉验收。测试没有发送游戏按键，也没有实际锁屏/睡眠。构建图形版候选包验证 Tcl/Tk 收录，继续遵守暂不覆盖根目录旧 EXE 的要求。另修复崩溃日志文件句柄生命周期，确保原生崩溃诊断文件在进程期间保持打开。

**候选发布**：图形版 EXE 已构建，预览模式启动待机检查、Tcl/Tk 资源及应用模块收录、ZIP 完整性均通过。SHA256：`65e78f1e48512f7d1c1601bebb2f69a66856f450b4a706f20a74cb645282dc09`。该候选包包含此前背包、多屏、计时与日志修复；根目录旧 EXE 尚未替换。真实自动聚焦及钓鱼流程留待用户允许更新后的实机验证。

**正式部署（17:34）**：用户明确要求打包后覆盖根目录。确认没有运行中的钓鱼程序后，已将上述图形版 EXE 与配套 `_internal/` 成套部署到根目录，逐一核对 1164 个文件的 SHA256，均与构建产物一致。原 EXE、依赖及配置备份在 `debug/release_backups/20260907_173436/`，该目录另存部署文件哈希清单。根目录配置字节级保持不变，自动清包仍为 `false`；原日志保留。此前暂缓部署的状态至此解除；本次没有启动真实钓鱼，实机验证限制仍如上所述。
