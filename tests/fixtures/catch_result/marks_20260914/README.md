# 尺寸与等级回归原图

十张未改色、未裁剪的成功截图来自本机历史诊断，文件哈希、ZIP／成员、轮次和归档 OCR 见 `provenance.json`。

`max_*` 覆盖六个独立 MAX 轮次；`new_record_*` 是黄色个人新纪录负例；`ordinary_day_*` 是白天普通尺寸及绿色／蓝色等级对照。没有 MIN 或彩色三级成功图。

运行识别没有从这些图提取整鱼模板；测试中的缩放和遮挡均为合成处理，不是额外实机样本。完整结论见 `docs/development/catch-markers-2026-09-14.md`。
