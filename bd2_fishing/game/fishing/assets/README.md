# QTE 反馈字形模板

`idle_cast_day_945.png` 来自 2026-09-09 18:00:22 旧 EXE 异常包
`event_1788948022285217000_b45b8197.zip` 的 `resume_latest.png`，
BGR `[391:513,803:909]` 裁剪，参考客户区 945×532。
补充白天深色抛竿轮廓，不降低 0.88 门槛；移动箭头仍须全部确认。
原图另存 catch_result 回归夹具；同包的黑屏 `resume_first.png` 为负例，不能用于模板。

2026-09-09 14:33 实测补充：`hit_compact_945.png` 来自 `3cb3471b-001` 第 1 次按键包
`qte_1788935596338157700_14c7788b.zip` 的 `frame_02.png`，BGR 裁剪 `[52:78,168:207]`。
用于完整紧凑 HIT 字形；不降低原白字匹配阈值。HIT 的强光备用匹配复用完整字形的纵向梯度，
要求边缘至少 0.85、同位置灰度至少 0.75；已有明确或歧义文字不被备用分支覆盖。
独立第 4 次按键原图及两个消失帧作为回归，来源见反馈夹具 README。

11:34:23 实测后：移动控件改为从既有 `idle_movement.png` 提取四个 11×11 箭头区域，
分别比较灰度字形和亮字形，不再比较透明按钮中的动态背景；抛竿控件仍单独确认。
箭头参考中心为 (142,402)、(110,435)、(175,435)、(142,467)，每个中心前后各五像素。
对应本次失败原图并未加入模板；四箭头和抛竿均需达到原 0.88 门槛，仍须跨帧确认。
旧 `idle_movement_*` 变体保留来源记录，当前控制不再加载这些背景变体。

`critical_plain_945.png` 来自同轮第 6 次按键反馈包 `qte_1788924884029809200_18378911.zip`
的 `frame_03.png`，BGR `[59:77,151:225]` 原样裁剪。补充清晰 CRITICAL 字形，
白字与强光备用路径仍沿用原阈值；同轮第 5 次按键的强光字样仍未可靠识别，未将背景光柱制作成模板。

`critical_early_945.png` 来自 12:18:10 源码实测 `a1d435a2-001` 第 7 次按键反馈包
`qte_1788927510112807400_d78c51b1.zip` 的 `frame_02.png`，BGR `[55:77,148:227]` 原样裁剪。
只在橙色完整字形补充识别中使用，与 `critical_plain_945` 共三个有限变体；模板 0.85、
同位置灰度 0.65 双重校验，不能以场景图标或命中区颜色代替反馈文字。
独立第二/第三轮字形与同轮空白、蓝光、图标负例见 qte_feedback 的 warm_* 回归图。

待机页新增 `idle_movement.png`、`idle_cast.png`：来自 2026-09-09 10:39:30
源码测试 `b3e84021-001` 的最终 `settlement.png`，参考客户区 945×532。
原图裁剪分别为 BGR `[390:482,98:187]`、`[391:513,803:909]`，仅转灰度保存。
两个控件必须同时达到 0.88，且跨至少 0.19 秒的两次观察均可见，才能允许续钓；
不从空白画面判断待机或捕获。模板尺度适配不代表其他机器和布局已实测通过。

`idle_*_night_945.png`、`idle_*_night_875.png`、`idle_*_day_875.png` 分别来自既有
`normal_945.png`、`normal.png`、`escaped_scene.png` 的对应控件。按同一比例裁剪并转灰度，
统一到上述 945 参考网格；不改变原始回归截图。当前移动控件仅使用前述四个箭头字形。
匹配忽略非有限分数，空白图不能确认待机。`idle_holdout_20260909.png` 未参与模板提取。

`settlement_close_945.png` 来自 2026-09-09 12:11:56 源码实测第一轮 `ba784f34-001`
的 `settlement.png`，BGR `[484:502,437:510]` 原样裁剪，参考客户区 945×532。
补充实际字体栅格化，仍使用 0.85 门槛和 ±1 像素范围；第二轮关闭前漏检原图仅作独立回归。

全部来自本机用户授权的 2026-09-08 Python 实机采样。下列旧模板的客户区为 875×492，只保留反馈文字小图：

- `critical.png`：`qte_live_20260908_110243` 恢复帧 128；`critical_alt.png`：`qte_live_20260908_105512` 第 3 次 QTE 按键后约 0.18 秒。
- `hit.png`：110243 恢复帧 32。
- `fail.png`：110243 恢复帧 72。
- `miss.png`：110020 色条外按键后约 0.1 秒。
- `settlement_close.png`：105512 录像约 35 秒的“点击画面关闭”，只供整条鱼结算模块识别面板，不参与 QTE 反馈匹配。

2026-09-09 从既有 945×532 原图补充 `hit_effect_945.png` 和 `hit_plain_945.png`：
分别为固定图册 U49-frame_02、U50-frame_03 的 HIT 字形，原 BGR `[51:75,168:203]` 裁剪，
不缩放、不改色。匹配器按各自参考客户区缩放，保留原 0.80 分数和 0.12 类别差值要求。
旧模板对这两帧仅得约 0.66，属于字形变体漏检；补充模板只能改善反馈识别，不直接确认按键归属或机制解除。
独立原图、同轮空白/FAIL/CRITICAL 负例及来源见 `tests/fixtures/qte_feedback/README.md`。

110243 因用户停止而没有完成 metadata 写入，仅从 ZIP 的完整本地文件记录恢复 PNG，可确认字形，不可恢复准确按键时间。QTE 字形匹配不推断真假指针、进度增减或最终捕获结果；结算关闭字形也只能说明面板出现，确认捕获还需要数量和尺寸证据。多分辨率、语言、未采样特殊机制尚未实机验证，未知文字应保留“未确认”。
