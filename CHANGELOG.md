# 更新记录

## 0.4.2 — 2026-09-14

- 修复圆角背景中心图块过小导致的重复平铺，减少页面绘制和空闲重绘开销，保留当前界面布局。
- 游戏识别模块改为启动任务时在工作线程加载，浏览图鉴和设置不提前导入 OpenCV／OCR。
- 图鉴首次打开才创建鱼卡，只解码当前可见图片；滚动时补齐，返回页面保留控件与滚动位置。
- 合并相同状态的页面刷新，鱼卡尺寸和状态未变化时复用已有绘制。
- 新增轻量启动导入回归、滚动末尾图片及页面复用检查。首次实际钓鱼仍需初始化识别资源，未据此宣称游戏启动零等待。

## 0.4.1 — 2026-09-14

- 按第四版原型重新整理实际桌面界面：顶部品牌栏、图标导航、浅色背景、圆角分区与更清楚的标题层级。
- 鱼卡分开绘制图片、名称和状态，网格与列表共用；图片填满预览区域，彩色鱼保留独立标记，已选目标有明确提示。
- 恢复钓鱼页的任务设置与进度分区，筛选项增加标签，列表详情与鱼名对齐，设置按设备和时延分区。
- 目标页原地进入选鱼流程，保存或取消后返回待办；保留 MAX／MIN、筛选和草稿规则。
- 鱼卡支持键盘操作和焦点保留，鼠标移出卡片后松开不会误选；缩略图缓存限制为 384 项。
- 本版调整界面，不改变 QTE 时序、游戏输入保护或鱼获识别规则；既有待验证边界保留。

## 0.4.0 — 2026-09-14

- 主界面整合钓鱼、图鉴、目标、鱼获和设置；底部启停常驻，保留全部设备与时延选项。
- 图鉴保留六岛 84 张离线鱼图和筛选，网格／列表用图标切换，窗口高度固定；列表在当前行展开与网格一致的详情，移除别名行与通用机制占位。
- 先选任意尺寸、MAX、MIN 或两者，再点击鱼卡多选；每条鱼可单独改条件。支持跨岛和按游戏时钟等待昼夜，完成条件从待办移除，全部完成后停止。
- 新增持久化带图鱼获，全部／首次／彩色／最大／最小分别筛选；同轮去重，旧鱼获不抵扣新任务。正常与恢复中迟到的奖励均先保存再关闭，写入失败停止并保留结算页。
- 真实截图确认红色 Maximum Size、黄色 New Record、绿色一级和蓝色二级，分别记录尺寸、个人新纪录、等级与边框颜色；补读低置信度鱼名，避免漏掉斗鱼 MAX。
- 按成功截图与同岛鱼图修正罗汉鱼／花羅漢、蝴蝶鱼／蝶魚的语言映射，四种客户端名称附核对证据。
- MIN 和彩色三级成功页仍待自然取证；跨岛任务的完整游戏效果由用户使用 EXE 验证。既有八项审查问题、冰冻与绿色起按端未因此标记解决。

## 0.3.12 — 本机测试版，2026-09-14

- 图鉴移除上一页／下一页，改为滚轮连续浏览全部筛选结果；左右区域独立滚动，支持小幅滚轮输入。
- 详情改为对齐的钓场、时段、稀有度、别名字段；机制说明改为简短的游戏描述，程序实现和来源备注移到“资料说明”。
- 右侧整块详情可滚动，小窗口也能查看全部机制；搜索和重置回到顶部，滚动不改变选中的鱼。

## 0.3.11 — 本机测试版，2026-09-14

- EXE 主界面加入“鱼种图鉴”，离线浏览六岛 84 种鱼图，支持繁简名称和译名搜索、钓场／昼夜／稀有度筛选、分页和机制详情。
- 图片仅显示鱼身区域，不显示攻略作者的个人尺寸纪录；点击图鉴不修改钓场、不创建目标任务或捕获进度。
- 运行中禁用图鉴入口，开始任务及退出助手时关闭图鉴；保留缺图占位、空结果和重复打开保护。
- 保留 Pillow Tk 图像组件并随包包含 84 张原图，新增图鉴预览和离线界面验收。目标鱼多选、成就记录和跨岛调度仍处于方案阶段。

## 0.3.10 — 本机测试版，2026-09-14

- 修复系统拒绝读取鼠标位置时主界面初始化失败：回退到助手自身窗口所在显示器，不影响游戏输入保护。
- 六钓场共用墙体检测、普通目标与机制仲裁；墙体同时约束泡泡和绿色持有，同帧只检测一次。
- 修复黄条纹理、压缩光标边缘被误识别成墙的问题，增加两份攻略的墙体正例和真实误检负例回归。
- 墙体与冰晶计数候选接入正常后台取证，保存出现、消失和未知结果；冰晶候选不直接触发破冰输入。
- 整理两份攻略、84 种鱼、繁简别名和 14 项可能机制，机制取来源并集；图鉴数据随包保留，图片多选、鱼获成就与跨岛目标任务仍处于方案阶段。
- 长期游戏反馈不作为本轮打包前置条件；自动破冰、绿色起按端及旧八项审查问题仍按原记录保留。验证和交付结果见[当前状态](docs/development/status.md)。

## 0.3.9 — 2026-09-14

- 绿色候选有明确位置但起按端未知时，仅避让其完整范围，继续处理分离的普通黄蓝目标；长按独占、遮挡和防重复输入保持有效，并增加对应控制帧及绿色范围记录。
- 合并机制协同与黄条分组修复，纳入本版构建；此前同源码 574 项完整离线回归通过。
- 修复真光标覆盖黄条时，孔内孤立残色被误作边界导致首次经过不按键的问题；重合与中心偏好共用实体分组。
- 两帧完整边界确认后，允许最多 180ms 的弱边缘辅助；当前两侧仍须有黄色，缺帧、变化、遮挡或过期即失效。
- 新增有界的黄区拒绝控制帧及判断依据；这两项新修复不在旧 0.3.8 EXE 中。
- 累计包含 0.3.2—0.3.8 的持续恢复、日夜弹窗、150302 分层关闭、六钓场目录、机制组合和更新检查限流改进。
- 更新 README、AGENTS、架构与开发文档，记录全量代码审查和历史问题验证范围。
- 已知限制：审查发现的 8 项问题仍待修复，涉及清包确认、手动工具保护、证据目录大小写、启动已有 150302、配置解析、更新结果缓存和输入释放失败处理。详见[审查报告](docs/development/code-review-2026-09-14.md)。
- 本版新 QTE 修复和弹窗原场景效果仍待实机验证；不宣称全部 MISS、未确认结果或特殊机制已解决。

## 0.3.8 — 本机测试版，未发布 GitHub

- 识别叠在鱼获页上的 `150302` 体力提示，动作前复核完整错误码和页面。提示与下层鱼获分别关闭，避免一次点击消耗错误层的关闭权限后永久等待；未生效按冷却重试。
- 560 项完整离线回归通过；150402 退出重进、普通鱼获防重复与窗口保护保持有效。游戏端效果仍待实测。

## 0.3.7 — 本机测试版，未发布 GitHub

- 计时颜色闪烁时，近期计时确认与当前实体目标、真光标共同支持最多 350 毫秒的接续；缺帧、缺目标和超时不放行。
- 已连续确认的泡泡附近短暂绿色残影改为局部避让，不再阻塞其他位置的黄蓝条；独立绿色机制与正在按住的动作仍保留原保护，并新增代表帧和范围记录。
- 修复普通真光标光晕分割黄条而漏按，仅在两侧连续实体、无机制阻断的小孔中恢复黄条连续性；不外扩目标边界。
- 修复夜间升级面板被归为普通鱼获：补充夜间标题字形，并分别核对钓鱼等级、活力、力量三行文字，避免行间背景干扰。
- 已确认升级页点击底部关闭提示位置；点击未生效时，间隔两秒最多补点两次，之后每三十秒重新确认后再尝试，避免一次点击后永久失去恢复权限。
- 升级重试记录落点、次数、前后页面与有界截图，待机必须经两次观察确认；普通鱼获防重复、停止和窗口保护保持有效。
- 上述修改均已重新构建：552 项完整离线回归、界面检查、1214 项包文件与 ZIP、EXE 预览和更新助手中断恢复验证通过；游戏效果待用户实测。

## 0.3.6 — 本机测试版，未发布 GitHub

- 多个泡泡分别确认与去重；绿色机制和不相交泡泡按输入状态协调处理，同帧最多一次按键。
- 增加真实贝壳遮挡模板，蓝条及黄条避开已确认障碍；挡板内光标不再投影到相邻可按区域。每轮有界保存机制组合代表帧。
- 收紧黄条实体边界，减少膨胀到条外的误按；保留普通黄条中心偏好与原有按住、松开时序。
- 拉竿后确认进入 QTE，对仍有咬钩提示的情况有限补按；修复部分鱼获数量漏读。
- 上述机制与命中优化已做离线回归，游戏端效果仍待持续实测。
- 更新检查改用官方 latest Release 跳转，移除匿名 REST API 查询依赖。
- 成功结果缓存一小时，手动刷新间隔至少一分钟；限流按 Retry-After / 重置时间冷却，无头部默认一小时，联网故障冷却五分钟，重启继续生效。
- 更新检查与日志/截图清理分别报告结果，网络失败不再误报为存储故障；本机测试版领先正式版时正确提示没有更新的正式版本。
- 保留官方版本固定下载地址、SHA-256 校验、包清单校验及本地 ZIP 更新入口。

## 0.3.5 — 本机测试版，未发布 GitHub

- 钓场目录补齐天空岛，界面、自动识别、导航和恢复共用六项目录；不再将它视为未知钓场。
- 天空岛暂用现有通用 QTE 策略并在启动时提示未实测；许可证仍须手动处理，专属机制等待真实样本。
- 修复白天升级页误归类为鱼获结算，支持已关闭本轮鱼获后单独关闭升级提示，保留关闭前证据并确认恢复待机。
- 包含天空岛目录和白天升级页识别修复；游戏端效果仍待用户实测。

## 0.3.4 — 本机修复候选，未发布

- 修复白天码头标题漏识别导致退出后持续重试：开始按钮和信息栏必须存在，标题不可读时同时核对船只管理与图鉴进度。
- 恢复阶段使用中文名称，失败的诊断日志及异常包保留页面判定、弹窗类型和码头各项识别依据。
- 加入 9 月 12 日 EXE 原图回归，保留原有两帧确认、窗口保护、按钮范围及 QTE 时序。

## 0.3.3 — 本机修复候选，未发布

- 将 150402 的退出重进扩展为通用钓场恢复：已确认钓场按 ESC、确认返回码头、重进原岛屿。
- 长时间等不到咬钩、位置调整仍受阻或恢复预算耗尽时尝试重进；已有退出确认框、码头、地图从当前步骤接续。
- 恢复中途失败保留任务，间隔至少 30 秒再试；已发送 bug 关闭等操作时不能因出现待机画面跳过完整重进。
- 正常等待优先等感叹号，已进行 QTE 不发送 ESC；未知弹窗不盲目确认，许可证不自动购买。

## 0.3.2 — 本机修复候选，未发布

- 删除连续未确认鱼获的计数停机，旧 max_unconfirmed_rounds 不再限制续钓。
- 等待页继续等咬钩，QTE 页接续当前鱼，待机页才重抛；运行中未知转场持续观察，恢复样本最多保留 300 项。
- 拉竿后始终没进入 QTE 时，3 秒后重新检查页面，避免占用完整 35 秒；已开始的 QTE 时序不变。
- 咬钩等待、抛竿前页面检查、调整位置后仍受阻均进入页面恢复；用户停止、窗口保护及满包关闭自动清理时的停止仍保留。

## 0.3.1 — 2026-09-11

本版包含下列修复及此前本机 0.3.0 的导航、页面接续与升级恢复。源码实测完成码头到亚特兰蒂斯导航和连续 3 条捕获、21 次暴击；黄条中心偏好触发 13 次延后。第四轮由测试时限中断。150402 退出重进及罕见组合机制本次未触发，不能视为全部验证通过。

- 新增有效矩形内随机点击：航海按钮使用当前 OCR 文字范围内缩后的落点，150402 关闭/返回确认复用该能力；记录实际屏幕坐标，保留原输入保护。未确认范围的操作仍使用已知点，QTE 空格时序不变。
- 拒绝 M13 咬人遮挡旁缺少实体支持的黄色残片按键，单轮缓存代表控制图。
- FAIL 独立记录为原因待确认，不直接归为按键 MISS，也不消耗当前按键的反馈等待窗口。后续 HIT/MISS 继续按原候选规则归属；只有 FAIL 时按键保持未确认，原历史包不改写。
- 增加普通黄条中心偏好：连续三帧确认稳定靠近目标、下一采样仍有空间时延后抢边按键；高速、反转、窄条或邻近机制时保留当前机会。保持既有输入时长与目标膨胀，新增瞄准诊断字段；实际命中率待验证。
- 修正 QTE 黄区残色误触发：颜色膨胀前先去除已知遮挡，并要求最低原始黄色像素支持；原图 M04/M06/M12/M14 不再授权原错误按键，有效黄区保留原膨胀范围及按住/松开时长。
- 新增稀疏黄像素拒绝的单轮代表帧与计数，用于后续 EXE 长期验证；QTE 剩余时序和机制归因仍未宣称解决。

- 针对游戏 `150402` 错误增加专用恢复：精确识别后关闭，按 Esc 并确认返回码头，再进入原钓场复核待机；普通导航不会自动确认返回。
- 明确 QTE 按键未命中、无对应按键 FAIL、反馈未确认三类取证。普通结算未确认后已返回待机时省略无诊断价值的结算/待机全屏图，保留 QTE 原帧与账本；页面异常和恢复仍保存现场。
- 普通导航与钓鱼已进行源码实测；具体特殊机制与恢复覆盖范围见本版验收记录。

## 0.3.0 — 2026-09-11（本机候选，尚未发布 GitHub）

- 自动换点的中转、返回统一使用页面导航，移除固定坐标选岛和确认；每段未确认到达时停止后续动作，保持窗口与取消保护。
- 允许当前岛名 OCR 漏读时通过已确认的更改入口进入地图，目标名仍严格核对；OCR 不可用或空读数不再触发自动换点。

- 区分换岛与返回码头确认；普通换岛核对目标后自动确认一次并等待到达，保存独立证据。新增仅导航源码验证模式，支持岛内“更改”入口。

- 源码启动新增码头、选岛和启航导航；页面与圆标重新识别后行动，禁止自动购买许可证，导航步骤与失败现场独立保存。
- 新增地图鱼种已解锁、未解锁、空白和未知读数；与岛屿许可证及推荐等级分别判断，不加入 QTE 逐帧路径。

- 更正水花按钮语义：属于等待咬钩，不能作为未抛竿；保留旧样本及标签修正记录。
- 启动与异常恢复支持接续等待；普通等待超时也先识别页面，未知或仍在等鱼时不再盲目移动/重抛。仅确认待机才重抛，已出现 QTE 则直接接续。

- 启动先识别页面：待机才允许抛竿，可关闭弹窗处理后复核，已有 QTE 接续当前鱼；未知画面限时观察并独立保存启动证据。
- 换点检查移至待机确认之后，首次抛竿和换点之后均重新核实页面；接续 QTE 不执行换点、抛竿或等待咬钩。
- 新增钓鱼升级提示识别，连续处理鱼获奖励与 LEVEL UP 弹窗，确认待机后继续下一条鱼。
- 关闭限制改为同轮每种已知弹窗各一次；保存弹窗身份、匹配依据、关闭时间与原图，延迟出现的升级提示可由恢复流程处理。

## 0.2.1 — 2026-09-10（本机候选，未发布）

- QTE 超时或结算观察异常后保留现场，结束输入与反馈观察，确认待机后继续下一条鱼。
- 补充无方向键、水花抛竿按钮布局识别；同时匹配按钮与 SPACE 字形，并排除活动倒计时。
- 恢复默认额外观察 10 秒，保留连续未确认上限和窗口/取消保护，关闭面板至多一次。
- 362 项离线回归通过；尚未完成此修复的 EXE 游戏实测。

## 0.2.0 — 2026-09-10

### 桌面与设置

- 调整任务和运行记录布局，以同一个按钮控制开始/停止；设备设置嵌入主窗口，建议值放在输入框旁。
- 更新应用图标、窗口定位、复选框及面向用户的日志展示。
- 基础等待校准支持无游戏窗口场景，分批采样 p95 与抖动，并独立给出检测/反馈间隔建议。

### 更新与数据

- 使用完整便携 ZIP 分发，新增正式 Release 检查、在线下载和本地 ZIP 导入。
- 更新助手等待主程序退出后执行备份、文件替换、旧依赖清理和失败恢复。
- 新版使用 config、data、logs、screenshots 和 cache 目录；首次启动初始化/导入并校验配置。
- 增加待机日志/截图保留策略及更新缓存清理，支持保留截图目录 keep。

### 钓鱼与维护

- 整合蓝区边界、明暗指针区分、泡泡/绿色场景规则以及机制仲裁和现场记录的离线改进。
- 完善反馈观察、结算识别、OCR 资源生命周期和停止后的资源释放。
- 新增真实截图回归、反馈包分类及回放工具，保留未确认结果与特殊场景证据。

### 验证与限制

- 发布前本机 346 项离线回归通过，隐藏 Tk 页面检查和真实更新助手的隔离更新/恢复通过。
- 本版仍有特殊机制和组合场景未完成实机覆盖，不能保证所有鱼捕获成功；基础校准不是游戏最佳时延。
- 当前更新只支持 Windows x64 正式版本的完整 ZIP，不提供 OTA、拖放、降级、静默安装或包签名。
- 旧版首次迁移方式及自动清包默认行为见[更新说明](docs/user/updating.md)和[使用指南](docs/user/usage.md)。

完整验证规划见[待验证问题](docs/development/validation-plan.md)。
