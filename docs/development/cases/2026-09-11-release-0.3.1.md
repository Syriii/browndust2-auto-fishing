# 2026-09-11：0.3.1 验收

本版汇总此前本机 0.3.0 的页面接续、码头/地图导航、自动换点和升级恢复，并补充按钮区域随机点击、黄区中心偏好、遮挡残色过滤、FAIL 归属与 150402 退出重进。

## 源码实测

- `qte_live_20260911_174905`：17:49:05—17:49:44，码头开始 → 搜索已解锁岛屿 → 亚特兰蒂斯启航 → 钓场待机。开始按钮实际落点 (2824,1037)，启航 (2752,1033)，均在本次 OCR 内缩区域中。没有购买许可证或抛竿。
- `qte_live_20260911_175050`：17:50:50—17:52:20，使用正式源码采集/输入路径，自动清包在测试进程中关闭，个人配置未改写。客户区位于副屏，窗口约 945×532。
- `53dbf9a9-001`：抛竿位置受阻，移动后恢复，7 次暴击，确认捕获 37.1 cm。
- `53dbf9a9-002`：7 次暴击，确认捕获 27.4 cm。
- `53dbf9a9-003`：7 次暴击，确认捕获 13.1 cm；鱼名 OCR 不完整，不补猜。
- 共 21 次暴击、0 HIT/MISS/FAIL；中心偏好触发 13 次延后。第四轮刚进入 QTE 即到测试时限，正常释放按键，不算失败或捕获。

该样本支持普通导航和钓鱼没有复现此前严格黄区内部规则的严重回退，不是长期命中率证明。无实测 150402、冰冻、绿色起按、多墙或复杂遮挡组合；不能将离线原图通过等同于机制解除成功。

## 离线与本机包

480 项完整回归通过；Tk 界面模拟、更新窗口模拟、Ruff、198 文件格式与 105 模块架构检查通过。代码质量工具最近整体约 84.49，人工复核和结果见本机维护报告。

本机候选包检查版本 0.3.1、清单 1208 项；实际更新助手完成隔离替换和中断恢复。实际 EXE 的 `--preview` 正常创建界面并退出，退出码 0，未启动游戏任务。

交付固定路径 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，完整分发包 `dist/BD2_AutoFishing-windows.zip` 与 `.zip.sha256`。旧固定包归档到外层 `archive/builds/20260911-before-0.3.1/`；个人 config/data/logs/screenshots/cache 共 263 个文件与归档原件逐一 SHA256 一致。候选预览生成的空目录曾导致复制嵌套，已由哈希核验发现并从完整归档纠正，未交付错误布局。

| 本机产物 | SHA256 |
| --- | --- |
| EXE | `774ba3240a2d2702fe5a2063a28e21947caebde81b65d71f984f432ac27c3dd8` |
| ZIP | `68082cf9f59b7325500b3c561e3dacaf6ae4bfcf14cc588c6508a1087b2364ad` |

发布通过 PR/CI 合并 main，再由 v0.3.1 的 Release 工作流构建公开 ZIP 与校验文件。GitHub 和本机二进制分别构建，哈希可以不同；下载应核对同一来源的 `.sha256`。完整发布操作记录保存在 `.local/maintenance/release-0.3.1/`。

## 仍有边界

仅蓝条、分身、泡泡已有基础处理，特殊叠加及真实时延仍需持续取证。FAIL 仅记录原事件与候选，不直接判为按键 MISS。150402 恢复仍可能因实际页面与样本差异失败。连续结果未确认仍受配置上限约束；窗口失焦、移动、锁屏、未知页面等保护停止保留。外层 deployment 未因本次更新 dist 而覆盖。
