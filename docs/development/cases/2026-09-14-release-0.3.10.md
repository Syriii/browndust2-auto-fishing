# 0.3.10 本机测试包验收

2026-09-14，按用户要求持续测试至 EXE 交付，长期监测不作为本轮完成门槛。此包未发布 GitHub，未覆盖外层 deployment。

## 使用入口

- 运行 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，保留同目录 `_internal/`、更新助手和清单。
- 分发包为 `dist/BD2_AutoFishing-windows.zip`，配套 `.sha256`。
- 已正常关闭两个助手并将新版归位至固定 dist；临时版本目录已移除。旧包归档至外层 `archive/builds/20260914-before-0.3.10-fixed-dist/`。
- 482 个旧用户文件逐项核对保留，5 个候选运行文件另行备份；个人配置和截图沿用，旧活动日志另存为 `logs/auto_fishing.pre-0.3.10.log`，新版日志保留原名。冲突的旧更新检查缓存保留在归档，不覆盖新缓存。分发 ZIP 保持无个人配置和运行数据。
- 固定路径交付再次通过 1220 项程序文件与 ZIP 校验；证据为 `fixed-dist-handoff.json`。

## 验证结果

- 603 项完整离线回归通过（91.998 秒）；Ruff、227 文件格式、115 模块架构、依赖一致性和 84 种图鉴文档一致性通过。
- 隐藏主界面、更新窗口检查通过；最终 EXE 预览完整初始化、正常关闭，退出码 0，没有运行游戏任务。
- 1220 项程序文件、ZIP 和 SHA-256 验证通过；真实更新助手在隔离目录完成替换和中断恢复，模拟个人配置保留。
- 全部 115 个 Python 包／模块（含独立更新助手）与当前源码编译结果逐一相等；图鉴 JSON、冰晶模板及其他玩法资源逐项核对哈希。代码比较只规范化编译文件路径。
- 构建额外发现并修复鼠标位置读取被系统拒绝时的窗口初始化失败：仅助手定位回退自身窗口显示器，正常情况下继续优先鼠标所在显示器。增加失败与正常路径回归；最终 EXE 已在同一受限环境成功启动退出。
- 质量扫描人工复核：钓鱼包最近结果 79.84/100；本轮构建脚本 92.25、窗口定位模块 92.13。定位模块的重复告警来自几何读取与原生调用的小型相似片段，不是本次回退导致的重复控制逻辑；低注释比例不视为功能缺陷。未新增游戏输入路径。

EXE SHA-256：`35a7bae878373b4c3dfb8e17de3995f067f6ec020b318901c6429186e79d18ae`。

ZIP SHA-256：`ee547ace2fd785eba73e78f4a9370211e811913d0aec3ebfa09231ee077577ad`。

完整证据保存在 `.local/maintenance/release-0.3.10-20260914/`。首次存在启动问题的候选完整保留于外层 `archive/builds/20260914-0.3.10-initial-preview-failed/`，不能使用该包作为最终交付。

## 能力与边界

本版包含六岛共用机制入口、墙体约束普通目标／泡泡／绿色持有、黄条纹理和压缩光标墙体误检修复、独立墙体与冰晶候选取证，以及两份攻略的 84 种鱼参考目录和繁简别名。鱼种机制取来源并集，布拉德保留泡泡可能性；输入仍依当前画面判断。

冰冻自动解除、绿色起按端、全部机制组合与天空岛游戏效果继续留待自然使用反馈。鱼获记录、图片多选、最大／最小目标及跨岛昼夜调度仍是已确认方案，本轮未实施产品 UI。原八项审查问题保留在[审查报告](../code-review-2026-09-14.md)，本次窗口定位修复不等于关闭旧审查项。
