# 下拉框显示修复 — 2026-09-15

clam 主题的 readonly+focus 状态使用白色文字，原先仅覆盖白色背景，导致任务模式选中并聚焦后看似空白。统一覆盖前景、背景和选中配色；焦点采用绿色边框，禁用项采用灰底。所有下拉框共用完整边框与居中单箭头，任务三行各增加上下间距。

源码可见预览的鼠标展开与选中、Down/Return 键盘确认及焦点文字通过。84 鱼图和筛选页面回归、默认/最小窗口本次鱼获布局、125%短屏与模拟启停通过。首次非交互桌面的图鉴遮挡断言失败，切换到可见桌面顺序执行后通过；原日志保留。证据在 `.local/maintenance/combobox-20260915/`。

固定本地 0.4.6 EXE 已检查展开菜单、选择后焦点文本、禁用项和待机布局。130 个模块与当前源码一致、1309 个程序文件核对，509 个原有用户文件按哈希保留；数据库 248 条鱼获、0 项目标和 1 项状态逐行一致。旧包归档到 `archive/builds/20260915-before-combobox-fix`，dist 仅保留固定四项。没有启动游戏任务。

EXE SHA-256：`e82ea895b9c454788eea5aab3e0ff08b2fe022972ab4e970a35e8ae8f12c3199`。
ZIP SHA-256：`344cfda4622674cbe5f209ffa5a4edabab5cca619f03ef90db6691fc183b1774`。

本次只更新本地包，GitHub 附件仍为 v0.4.5，deployment 未覆盖。
