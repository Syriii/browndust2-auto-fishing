# a1d435a2：六份反馈未确认复核

原始来源：2026-09-09 12:18:10 源码测试，基于 `e2731fb` 的普通页面修复。
此处 U01–U06 只属于本次样本，不与旧 60 包的 U 编号混用。
原测试 31 次输入为 25 次暴击、6 次 unknown；两轮捕获、第三轮限时中断。
下表是维护回放结果，原账本、时间戳和 ZIP 不改写。

**分类更正**：下表描述反馈识别问题，不是普通/特殊场景的互斥分类。
U03、U05、U06 的保留帧均可见紫色图示；U06 字形漏检的修复不代表对应特殊机制已解决。
U04 的选中反馈帧未明显呈现紫色图示，但其所属第二轮场景序列有紫色阶段，不能把整轮标为普通。
U01/U02 未在这些选中帧确认特殊图示，不据此推断完整游戏过程没有机制。
用户要求总览同时列出紫色、绿色、被咬住三类已有样本：绿色来自此前 b3e84021，
被咬住来自 f43f14e2 的牙齿原图，不能遗漏，也不能改写为本轮新采集；详见[机制总览](../../design/special-mechanism-handling.md)。

| 本次编号 | 轮次 / 按键 | 原图证据 | 分类及处理 |
| --- | --- | --- | --- |
| U01 | 001 / 4 | frame_02，+172ms，CRITICAL 可见 | 字形漏检；完整橙色描边及灰度校验通过，0.957，独立样本 |
| U02 | 001 / 7 | frame_02，+47ms，CRITICAL 可见 | 早期动画字形漏检；此帧为新增模板来源，1.000 不代表独立泛化效果 |
| U03 | 002 / 3 | 750ms 窗内无确认文字，场景含紫色图示 | 保留 unknown，特殊机制暂缓；+1031ms 的 CRITICAL 已在下一次按键（+890ms）之后，不回填 |
| U04 | 002 / 10 | frame_02，+46ms，CRITICAL 可见 | 字形漏检；新增早期字形在独立第二轮通过，0.960 |
| U05 | 003 / 8 | frame_03，+203ms，CRITICAL 与提示图标、光效叠加 | 可见文字但未可靠识别，保留 unknown；不裁成半个词或降低门槛 |
| U06 | 003 / 9 | frame_02，+47ms，CRITICAL 可见 | 字形漏检；早期字形取整变体在独立第三轮通过，0.852 |

六项原归属窗内均有 12–14 次观察，最大帧间隔约 63–94ms，账本类别均为 feedback_not_detected。
这排除了完全没有观察的情况，不代表每个游戏显示帧都已采集或保存。
观察包按既定策略各保留 8 张原图；不能利用窗外下一次按键的反馈补写当前结果。

实现使用完整 CRITICAL 橙色描边的红蓝差值，先筛选红色占优的候选区域，再作模板相关与同位置灰度复核。
模板相关至少 0.85、灰度至少 0.65，仅用三个有限字形变体。原白字匹配及类别差值规则优先，
新方法只在原最高匹配不足 0.80 时补充；特殊机制控制、按键时序、750ms 归属窗均不改动。
新模板取自 U02 的 BGR `[55:77,148:227]`；U01/U04/U06 未参与模板提取。
同轮空白、蓝光、紫色图示与橙色提示图标作为负例；U05 保留遮挡样本。

最终实现的离线耗时对照：48 张原帧，交替执行新旧识别，每版剔除预热后 336 次。
旧版平均 12.926ms、p95 14.611ms，新版平均 12.977ms、p95 15.456ms。
这是局部图像匹配耗时，不含采集、观察线程调度、输入或游戏响应，不能当作命中率或端到端提速证据。

本机图册：`.local/maintenance/feedback-review-121810/index.html`，48 张原图可逐张打开；
`replay.json` 保留旧识别和元数据，`comparison.json` 保留新旧对照。
原始六个 ZIP、完整轮次日志和账本位于 `.local/maintenance/live-review-20260909-121810/`。
实机验证的页面检查未能聚焦游戏，未开始新的钓鱼；本次修复等待实机验证，尚未提交或部署。
