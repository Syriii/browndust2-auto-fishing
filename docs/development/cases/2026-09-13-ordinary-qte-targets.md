# 普通 QTE 目标漏按复核

用户反馈没有特殊机制时黄蓝条似乎无法识别。本次冻结 `dist/BD2_AutoFishing/logs/auto_fishing.log` 与最近八份反馈包，未操作游戏。原记录、逐帧回放及图像索引位于 `.local/maintenance/qte-targets-20260913/`。

## 结论与范围

- 14:30—15:47 的 132 份轮次按键汇总共有 933 次尝试、834 暴击、76 普通命中、23 未确认、0 个归属于按键的 MISS。范围含中断轮次，不能将 132 当作鱼获数，也不能将 FAIL 当作按键 MISS。
- 最近八份包的 64 张反馈帧回放中，58 张仍有可确认黄色实体段、47 张仍有蓝色安全段；不是黄蓝识别整体失效。这是独立反馈采样，不能代替没有保存的控制帧。
- `22f3e0a0-283` 的 `qte_1789285586016417500_53be653b.zip/frame_04.png`：真光标位于普通黄条内部，生产策略原图回放拒绝输入。原因是白色光晕覆盖 13 列，超过旧修孔上限 9 列，只保留右侧残段而误判光标在黄区之外。此帧无已识别机制，符合用户指出的普通场景。
- 部分反馈特效帧仍有光标和目标区，但旧计时颜色检测为 0，运行时会跳过输入。证据显示该现象存在，但它对某次漏按的贡献尚无完整控制帧序列证实，不直接放宽 QTE 在场判定。
- 15:41 的 `22f3e0a0-273` 没有任何按键尝试；截图包含泡泡与后续残影，属于另一类问题，不能作为“没有特殊机制”的例证。

最初怀疑控制 ROI 高度影响实体阈值，随后沿生产 `_split_roi_and_time` 路径确认实际条体为 19×244，已排除此推测。

## 源码修复

在原 35% 竖向实体支持不变的前提下，光标内部孔洞上限调整为条体高度的 75%，并新增两侧连续支持及机制阻断检查。只修补光标所在的小孔，不扩大外边界、不从单側残片推断条体；黄条中心偏好与原按住/松开时序保持不变。

`supported_yellow_overlap` 与 `YellowAim` 共用带 forbidden 范围的识别，泡泡、红紫遮挡和当前有效范围外的区域不能作为孔洞桥接。真实问题图在两种策略中恰好触发一次黄条按键，重复观察不重复按。

此次只修复已由原图证实的黄条光晕问题。完全遮挡、边界仅剩单侧颜色、计时反馈特效期间输入是否可继续、泡泡原图对应的连续轨迹仍需后续复核。未游戏实测，未更新 EXE 或发布 GitHub；先前 0.3.7 升级页候选尚不包含本次改动。

544 项完整离线回归、Ruff/格式和 108 模块架构检查通过。质量工具总分 83.79，人工复核包括禁区桥接、重复入区与输入时序。没有新增逐帧磁盘、截图、OCR 或等待。
