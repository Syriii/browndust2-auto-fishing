# 2026-09-11：0.3.0 候选验收

本次交付汇总导航、自动换点、等待/QTE 接续、升级和异常恢复；GitHub 发布与本机 deployment 不在本次操作范围。

## 开发与源码测试

- 已完成码头到地图、岛屿内更改、换岛确认，以及亚特兰蒂斯经烟波湖往返验证，详见同日航海记录。
- 新采样 4f2002fe-002 的 28 帧组合外观及无对应按键的 FAIL 已归档。尝试严格黄区内部判定后，13:26:45 的前两轮共 14 次失败反馈、0 暴击；候选已撤回，保留原控制。不能仅凭决策帧在黄区外推断实际输入生效位置。
- 最终源码 `qte_live_20260911_132941`（13:29:41—13:31:11）连续确认三轮捕获，尺寸分别 23.2、21.0、27.4 cm，每轮 7 暴击；鱼名存在 OCR 漏字，不补猜名称。第二轮位置受阻后自动调整并捕获。第四轮上钩后由 90 秒工具时限中断，不计捕获。
- 433 项全量回归、Ruff、187 文件格式、101 模块架构检查通过。隐藏界面冒烟覆盖启停、设置保存/取消、日志和关闭；更新窗口检查覆盖忙碌状态、网络失败回退和设置保持，不连接游戏。
- 整体质量工具评分 84.64。

## 打包与交付验证

- 便携包版本 0.3.0，清单 1206 项；主程序/更新助手中 101 个源码模块完整，29 张识别模板与源码哈希一致，3 个 OCR 模型齐全。
- 更新助手在隔离测试目录完成替换与中断恢复检查。实际 EXE 的 `--preview` 窗口正常打开并关闭，退出码 0；这些检查不向游戏发送输入。
- 交付路径：`dist/BD2_AutoFishing/BD2_AutoFishing.exe`；完整分发包：`dist/BD2_AutoFishing-windows.zip`，旁边附 `.sha256`。
- 原本机包完整归档至工作区 `archive/builds/20260911-before-0.3.0/`；原 `config/`、`data/`、`logs/`、`screenshots/` 共 145 个文件逐一核验 SHA256 后保留。分发 ZIP 在恢复用户数据前生成，不包含这些个人运行记录。
- 校验报告存于 `.local/maintenance/release-0.3.0/`：`package-check.json`、`resource-check.json`、`preview-check.json`、`handoff.json`。

| 文件 | SHA256 |
| --- | --- |
| EXE | `f759bf88140a05fc1fafe79729e200f2f323a8c655f036d8eb62977f0e172216` |
| ZIP | `cd73daf7ba6c1214df1ce65423e0465ad8f5157bf99147895b58528ccaf8ad9e` |

## 验证边界

黄区时延、泡泡/贝壳等组合机制、真实拖图搜索及其他岛屿往返仍需继续针对性采样。本次三轮普通捕获不是全部机制通过的证明。游戏实測使用 Python 源码；EXE 的资源、更新助手和启动验证另外记录，不冒充 EXE 游戏实测。
