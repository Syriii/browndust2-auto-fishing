# 2026-09-11：码头、选岛与鱼种状态实测

[当前状态](../status.md) · [识别规则与原图](../../reference/voyage-navigation.md)

## 结论

用户要求测试新提供的入口和地图内容，不能只验证原有钓鱼过程。最初源码在码头连续 98 次返回未知并停止，随后补充 `game/navigation/voyage_reading.py` 和 `voyage.py`，接入 `app/session.py` 启动流程。

实测使用 Python 源码，客户区 945×532，位于第二块显示器。自动清包在本次进程内关闭，未修改用户配置，未更新 EXE、提交或发布。导航不购买许可证。

## 实测经过与证据

| 时段 / 记录 | 结果 |
| --- | --- |
| 11:13，`qte_live_20260911_111340` | 原代码无法识别码头；未抛竿，保存启动首尾两帧和初始化现场 |
| 11:29:59—11:31:29，`qte_live_20260911_112959` | 识别码头 → 打开地图 → 确认烟波湖、寒霜海峡、浅岸、亚特兰蒂斯 → 启航 → 识别可抛竿页面 |
| 同次导航 | 共 5 次点击：开始钓鱼、3 次选岛、启航；没有购买或拖图操作。实机读取亚特兰蒂斯鱼种 13 已解锁、2 未解锁 |
| 同次首轮钓鱼 | 首次上钩等待超时、峰值黄色像素 0；复核待机后重试，识别到位置受阻提示，调整位置后成功上钩。记录 10 暴击、6 普通命中，因工具 90 秒时限中断，未确认鱼获 |
| 11:32:09—11:33:39，`qte_live_20260911_113209` | 三轮确认捕获：魔鬼鱼×1（472.8 cm）、丝带鳗×1（137.4 cm）、蝴蝶鱼×1（22.6 cm），结算后继续下一轮 |

第二次诊断的三轮反馈分别为 13 暴击/1 未命中、12 暴击/1 普通命中/1 次按键反馈未确认、7 暴击。一次成功捕获不等于所有按键都命中，也不表示特殊机制全部验证完成。

现场保存在本机 `.local/maintenance/navigation-20260911/`：导航 ZIP、各步骤 PNG、两个会话日志与按键时序、离线测试日志和质量报告。正式导航诊断归 `.local/diagnostics/navigation/`，截图和页面读数随包保存；停止后不重新截取桌面。

## 离线与质量验证

- 七张用户原图进行真实 OCR 回归；许可购买前/后、红色推荐等级、鱼种状态和空格分别验证。原图哈希保持不变，客户区裁剪范围已记录。
- 411 项完整回归通过；之后仅拆分识别函数并补充拖动释放测试，18 项相关回归再次通过。
- Ruff 检查通过、184 个 Python 文件格式通过、100 个模块的依赖边界及静态循环检查通过。
- 质量工具整体 84.78/100；导航识别 90.27、导航协调 86.71。识别函数最大圈复杂度从 21 降至 11，未改变已验证判定条件。

## 尚未覆盖

- 本次目标在地图可见，未触发拖动搜索。拖动的停止、异常释放、终点及参数边界已离线验证，真实拖图后的重定位仍待实测。
- 天空岛购买前仅使用不可逆操作前的原图验证；不重复购买。天空岛尚无钓鱼策略，因此导航不会自动启航到该岛。
- 启航中的实际过场暂归 unknown，有界等待后到达；封面 loading 分类通过离线原图，不能算实机过场识别通过。
- 鱼种网格目前覆盖样本的三行五列布局；浅岸推荐等级数字存在 OCR 漏读，保留未知，不猜测为 7。
- 首次抛竿未成功的原因还需结合提示时序继续分析；本次验证了随后待机复核、明确受阻处理及恢复上钩，不能据此宣称第一次抛竿问题已修复。

游戏主页到码头、完整背包页面、天空岛策略仍不在本次实现范围。已部署 EXE 继续保持原版本。

## 后续：岛内更改与换岛确认

12:14:36 的 `qte_live_20260911_121436` 从地图选择深渊巨口并点击启航，遇到“前往钓鱼地区”确认框；当时未识别该类型，随后因失焦保护停止，未点击确认。该帧归入 `controls/travel_consumables_confirmation.png`。用户随后补充另一尺寸原图，明确说明这是普通换岛确认，多数情况下可直接确认。

源码随后区分返回码头与换岛确认：标题和正文模板用于快速拦住背景误判，导航 OCR 再读取目的地及按钮。连续两次识别一致且目的地符合目标后确认一次；没有目的地、目标不符、自动确认关闭时不点击，同一弹窗不重复确认。

| 源码会话 | 实际结果 | 操作 |
| --- | --- | --- |
| `qte_live_20260911_122517`，12:25:17—12:25:41 | 地图启航后读到深渊巨口确认，12:25:29 确认，12:25:41 到达待机 | 启航 1 次、确认 1 次 |
| `qte_live_20260911_122559`，12:25:59—12:26:56 | 深渊巨口待机 → 更改 → 地图搜索 → 亚特兰蒂斯 → 启航 → 确认 → 到达待机 | 更改 1 次、选岛 5 次、启航 1 次、确认 1 次 |

两次使用 `--navigation-only --no-full-frames`，到达即停止，未抛竿、购买、点击船锚或返回码头。最终原帧人工核对地点标题与待机控件一致，游戏留在亚特兰蒂斯。搜索仅使用可见圆标，没有触发拖图。

对应导航证据分别为 `event_1789100741489623900_b5d879b1.zip`、`event_1789100816662337500_37e20ae8.zip`；页面读数、动作前截图和最终帧保存在 `.local/diagnostics/navigation/`，维护副本归 `.local/maintenance/navigation-change-20260911/`。

425 项完整回归通过。其后仅抽取导航到达分支为独立函数，并让确认框样本同样限制最近 80 帧读数，24 项导航专项再次通过。Ruff、186 文件格式、101 模块架构检查通过。质量评估整体 84.76，导航 84.55（最大圈复杂度 14、认知复杂度 19）、导航识别 89.91、确认框识别 96.19；报告见本机维护目录 `quality-review.md`。

12:26 阶段尚未迁移 `game/islands/travel.py` 的旧换点路径；后续迁移与往返验证见下节。未提交、发布或更新 EXE。

## 后续：正式自动换点迁移及返回后钓鱼（13:03—13:10）

`game/islands/travel.py` 移除固定坐标、中间无条件确认和固定加载等待，仅保留中转路线。每段调用同一 `prepare_voyage`，确认中转到达后才开始返回；返回到达后由 FishingBot 再次复核待机才能抛竿。停止异常直接传播，导航失败包装为换点错误且保留原原因。五个原钓场的往返路线、两段失败、OCR 不可用、自动确认关闭、两段之间停止均有回归。

13:03:09 `qte_live_20260911_130309` 出发前拒识，未点击。原帧实际是亚特兰蒂斯待机，OCR 将名称误读为“正特兰蒂斯”，但更改、使用时间和待机控件可识别。现保留岛名未知，允许打开地图再核对目标；不为此添加猜测别名。该帧归入 `controls/island_day_name_unreadable.png`。显式导航从第一帧开始取证，出发前拒识也不丢失导航现场。

| 会话 | 结果 |
| --- | --- |
| `qte_live_20260911_130724` | 13:07:29 更改；13:08:09 确认前往烟波湖；13:08:20 到达。随后打开地图，13:08:45 确认亚特兰蒂斯，13:08:56 返回并结束往返测试 |
| 同次动作 | 去程：更改 1、选岛 6、启航 1、确认 1；返程：更改 1、选岛 3、启航 1、确认 1。未拖图、购买或返回码头 |
| `qte_live_20260911_130925` 第一轮 | 返回后首次抛竿有明确位置受阻提示，调整后上钩；13 暴击/1 未命中，确认捕获锯鲨×1，163.7 cm |
| 同会话第二轮 `4f2002fe-002` | 5 暴击、2 普通命中、6 失败反馈、2 次按键反馈未确认；未取得明确鱼获结算，复核待机后继续第三轮 |
| 同会话第三轮 | 已上钩并记录 1 暴击，13:10:55 达到 90 秒测试上限后中断并释放按键；不算完成捕获 |

第二轮失败包 `event_1789103443365905300_f7256e7a.zip`，场景候选分三包保存，维护目录 `.local/maintenance/island-refresh-20260911/` 保留副本和 `qte-review-4f2002fe-002.html` 的 28 帧时间线。蓝条无黄条与泡泡/倒数外观出现；部分 FAIL 未归属到按键，不能把全部失败解释为操作偏移。红/绿/紫文件名只是像素候选，不据此确认具体机制。本次未改 QTE 阈值或输入时序。

两段导航证据为 `event_1789103300764329700_b78a9d11.zip`、`event_1789103336467187400_9bf5dabc.zip`。433 项完整回归通过，Ruff、186 文件格式和 101 模块架构检查通过。质量工具整体 84.63，换点策略 90.36、地点读取 92.48，详见本机维护目录 `quality-review.md`。

尚待覆盖：真实拖图搜索、其他原钓场的往返实测、天空岛策略、自动到期触发、第二轮组合机制。现有缺“时”的可选规则不等同于准确到期检测，默认仍关闭；已修复 OCR 不可用/空读数误触发。未修改个人配置、未构建或更新 EXE。
