# 0.4.4 常驻日志、密集图鉴与鱼获修复

2026-09-15；日志阶段证据 `.local/maintenance/run-logs-20260915/`，最终图鉴／鱼获及交付证据 `.local/maintenance/catch-records-20260915/`。

## 布局与行为

钓鱼页恢复常驻运行日志框，取消展开／收起入口。任务设置与本次进度并排，保留日志筛选、自动滚动、日志目录、异常截图和更新与存储入口。仍使用原有有界日志队列、1500 条进展副本及文件日志，不增加后台线程或改变游戏输入。

默认 1100×760 下文字区域高 206px，最小 960×640 下为 86px。实际 Tk 截图见 `default.png`／`minimum.png`；125% 短屏检查覆盖日志、选项和底部按钮边界。进度摘要随可用宽度换行。

## 本地检查

628 项完整回归通过（154.466 秒），Ruff、248 文件格式、128 模块架构通过。默认与短屏可见冒烟覆盖日志筛选、迟到记录顺序、设置草稿及两轮模拟任务启停；图鉴可见冒烟覆盖首次绘制、列表末尾、筛选和 MAX／MIN。代码质量扫描 128 文件，83.59/100，相关告警已人工复核。

最终版本 637 项完整回归通过（111.228 秒）、251 文件格式和 129 模块架构通过。图鉴可见冒烟追加网格／列表统一详情、同尺寸图鉴小图、未知鱼基础字段和浏览不改写记录／待办检查。构建、固定目录交付与官方发布核验待完成。本次未启动真实游戏任务，原审查问题及需日常反馈的机制边界保持原状态。

## 密集图鉴与鱼获资料

按用户最终确认采用左侧密集小图或列表、右侧固定详情。网格按可用宽度排列，默认窗口约 8 列、最小窗口约 6 列；详情独立滚动。统一图鉴图片按比例居中缩放，不再混排结算图标或把鱼图裁成宽横条。切换视图保留所选记录。

成功原图核实蓝龙海神鳃／海蛞蝓、鸳鸯鱼／七彩麒麟鱼、鲳鱼／银鲳的映射；蓝龙海神的明确 OCR 漏字别名保留。名称只清理 Unicode、空白和数量后缀，不做模糊补字。鲳鱼参考图标由一轮原图生成，另两轮独立正例通过；所检其他鱼负例不匹配。该图像规则只关联历史资料，不参与游戏输入或目标确认。

本机 25 条鱼获均能关联到图鉴，其中三条只剩“鱼×1”的鲳鱼使用已核实图标匹配。截图和数据库副本仅作本地验证，原记录、首次分类、MAX／MIN 与目标不被显示关联改写。未知鱼显示已有捕获信息和待补充状态，原结算证据继续保存，后续可据此完善图鉴。

## 日期与时间

鱼获使用捕获时间倒序，本地日期分组与下拉筛选，日期条件和成就分类同时生效；异常时间单列未确认。网格显示尺寸、时分与 MAX／MIN，列表保留时分，固定详情显示完整时间。新增三项回归验证跨时区时间顺序、日期与成就组合、异常时间可访问；可见 Tk 检查覆盖两天记录、日期切换、空结果、网格／列表保留选择与原始历史不变。

最终代码质量扫描 129 文件、83.41/100；无低于 60 分文件。报告与人工复核见本地证据 `quality.json` 和 `quality-review.md`。
