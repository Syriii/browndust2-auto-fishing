# 0.4.8 紧凑本次鱼获与神仙鱼缺图

证据：`.local/maintenance/compact-catches-20260915/`。

## 修改

- 小窗外边距 12 → 8 px，四列图卡，每张 58 px 高（旧版 90 px，另有网格间距）。
- 每张保留鱼图、尺寸、等级与 MAX/MIN；时间在点击后的完整详情显示。
- 去掉内层边框和底部大按钮，“全部 ›”入口与计数同排。
- 以鱼获 ID 增量维护图卡；同一运行新增一条，不再销毁整窗已有卡片；仍最多显示当前 run_id 最新 30 条。
- 第 252 条旧记录读为“神仙鱼x1”，图鉴只登记天使鱼，导致无图。核对 14.2 cm 真实结算图与 fish_05_04 图鉴外观后补充客户端名；保留繁体与旧译名。图鉴、历史、本次鱼获和后续准确 OCR 共用该名称表。
- 其他尚未确认的鱼显示记录名称，不用孤立问号掩盖原因；不猜测身份，不回写旧鱼获或完成目标。

## 验证

22 项名称、目录、历史、轮次过滤回归通过；追加神仙鱼高／低置信度解析检查也通过。真实 Tk 检查默认 1100×760／最小 960×640、神仙鱼显示图片、四列与 58 px 高度、既有卡片保留、30 张上限、点击历史详情、下一次启动清空、设置保存通过。

Ruff、258 文件格式检查、131 模块架构通过。本轮是局部修改，未重复全部游戏回归。桌面截图两次失败，原生窗口操作返回 GetCursorPos 拒绝访问，不能把几何断言通过写成截图验收。

## 交付

源码为 0.4.8。最终候选包已通过 131 个内嵌模块、1309 项清单文件、129 项玩法资源及真实更新助手／中断恢复检查。候选放在 `.local/maintenance/compact-catches-20260915/candidate/`，避免在固定 dist 增加交付子目录。

当前固定 EXE 仍为 0.4.7，进程未退出，尚未替换。已向用户请求退出助手；退出后使用同目录 `promote-fixed-dist.ps1` 成套归位，先备份并验证用户配置、数据库和日志。未覆盖外层 deployment，未发布线上 Release。

- 候选 EXE SHA-256：`80e9408cb646149b1b8272f9b3477e5b22a5199417a6bc2115d67709e722c435`
- ZIP SHA-256：`daa2577f410c92df6a61ae4f32a98ece365f0c8d53511bd3d56433eb1692e5f3`
