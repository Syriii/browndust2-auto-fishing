# 特殊场景案例：b3e84021-001

[问题清单](../error-triage.md) · [当前状态](../status.md) · [特殊机制方案](../../design/special-mechanism-handling.md)

## 来源与结论边界

2026-09-09 10:39:30 的源码测试，处于设备与时延设置功能开发期间。
进程内使用按住 80ms、松开后等待 160ms，检测与反馈观察间隔各 20ms；未覆盖个人配置。
用户确认本轮出现多个特殊机制且有未命中；本记录将可见画面与结果分开，具体机制身份、操作规则及失败因果尚未逐项确认。
本次整理没有再次操作游戏，也没有修改控制策略。

10 次 QTE 输入中，7 次确认 CRITICAL、2 次确认 HIT、1 次 unknown。
另记录 4 次没有关联新输入的 FAIL；不能将结果账本的 14 条记录解释为 14 次按键。
结束后 19 次有界结算检查仍无奖励面板，整条鱼保持 unknown，不能记录为成功捕获。
本轮场景和此前普通场景不同，不能直接用它比较时延参数优劣。

## 原始证据与图册

固定案例编号为 `20260909-103930-b3e84021`，原始轮次为 `b3e84021-001`。
本机案例存放在 `.local/maintenance/cases/20260909-103930-b3e84021/`，与诊断轮转目录分离。

- `originals/`：完整复制 10 个 ZIP、测试摘要、日志和原核验结果，共 13 个文件；原 ZIP 内共 250 张 PNG。
- `images/`：直接提取 114 张原图，包含 71 张场景图、40 张反馈图、1 张控制决策图、2 张结算图；没有增强、重绘或覆盖原包。
- `index.html`：本机离线图册，列出全部输入、11 次场景事件、完整保留的场景帧、未确认包和四个 FAIL 包。
- `manifest.json`：原包完整元数据、来源、精确时间、SHA-256 和提取图片索引。

原测试目录 `.local/maintenance/live-review-20260909-103930/` 仍保留。
本地图片不随 Git 文档发布；后续确定规则和断言后，再选择必要正负例进入 `tests/fixtures/`。

场景缓存保留 71 帧，11 个事件时刻的原图全部存在；另淘汰/拒收 32 帧：
7 张旧普通时间线、18 张旧候选上下文、7 张新普通时间线。
采样断档计数为 0 不代表完整录像，早期事件的前后画面仍不完整。

## 全部场景事件

下列 S 编号仅在本案例内使用；11 次外观事件可以包含相同或叠加机制，不等于 11 种技能。
时间为本次进程的 monotonic 秒，不能作为墙钟或与其他进程直接比较；图册同时显示相对首次输入的时间。
图片路径相对于案例的 `images/`。带“算法线索”的描述尚不能直接作为技能身份。

| 编号 | 时间 | 原图 | 画面或算法线索 | 待评估事项 |
| --- | ---: | --- | --- | --- |
| S01 | 94237.312 | scene-frame_000.png | 可见蓝区与绿区，没有黄区 | 已取得绿区实图；蓝区与绿区分别承担什么作用、何时切换输入模式 |
| S02 | 94238.234 | scene-frame_001.png | 蓝区、绿区、紫色图示同时可见；算法记录两处光标候选 | 区分真光标、分身、色区边界和图示遮挡；不直接认定为分身 |
| S03 | 94239.140 | scene-frame_002.png | 可见紫色图示、蓝黄区；算法未得到可信光标形状 | 原图仍有竖线特征，排查遮挡/定位，不直接归为隐形 |
| S04 | 94242.921 | scene-frame_010.png | 紫色内容候选，可信光标 x=185 | 紫色图示与有效输入区、距离变化的关系 |
| S05 | 94243.671 | scene-frame_018.png | 紫色内容、目标存在但无可信光标的算法线索 | 与相邻原图对照，确定定位丢失阶段 |
| S06 | 94244.890 | scene-frame_026.png | 紫色内容、目标存在但无可信光标的算法线索 | 位于第 9 次输入之前，核对重新定位与按键依据 |
| S07 | 94245.515 | scene-frame_034.png | 紫色内容候选，可信光标 x=230 | 位于第 9 次输入的观察阶段，核对画面反馈和位置变化 |
| S08 | 94248.421 | scene-frame_042.png | 紫色内容候选，可信光标 x=167 | 核对连续 FAIL 期间可命中的条件 |
| S09 | 94249.156 | scene-frame_050.png | 可见黄区，没有可见蓝区，紫色图示仍在 | 区分锁区、过渡与遮挡；尚未认定为“锁普通命中区” |
| S10 | 94249.765 | scene-frame_058.png | 可见绿区和紫色图示，没有可见蓝黄区 | 绿区独立阶段已有实图，进入、维持、退出的控制规则待确认 |
| S11 | 94251.421 | scene-frame_066.png | 紫色内容候选，可信光标 x=123 | 与第 10 次输入及 CRITICAL 对照，核对后续恢复阶段 |

上表光标 x 是元数据所用观察 ROI 的局部坐标，不能直接用作屏幕点击坐标或与控制 ROI 的 x 混用。
S01/S02/S10 使绿色机制的状态从“未取得相关画面”更新为“已有绿区画面，待确认规则和连续序列”。
不能仅凭绿色就启用按住控制，也不能把蓝绿混合阶段直接按 U07 的蓝区单击规则处理。

## 全部未确认与 FAIL 包

为避免与旧图册 F01–F06、U01–U54 混淆，本轮使用 R39 前缀。
每个反馈包均保留 8 张观察原图；第 9 次输入还保留同次控制决策原图。

| 编号 | 时间 | 包名 | 记录及限制 |
| --- | ---: | --- | --- |
| R39-U1 | 94245.734 | qte_1788921598483845500_91dfdd89.zip | 第 9 次输入于 94244.953 发出，结果为反馈未检测到；观察窗口 0.75 秒，17 个有效观察，最大采样间隔约 63ms。不能据此认定系统丢键或确定 MISS |
| R39-F1 | 94245.937 | qte_1788921598664724400_dadb7bba.zip | FAIL，距第 9 次输入约 0.984 秒，已在其归属窗口外；暂不回填给第 9 次输入 |
| R39-F2 | 94247.578 | qte_1788921600317429100_6cff8563.zip | FAIL，没有关联新按键；核对当时目标区和机制要求 |
| R39-F3 | 94249.203 | qte_1788921601927488100_728cfae8.zip | FAIL，邻近 S09 的仅黄区阶段；时间相邻不证明因果 |
| R39-F4 | 94250.812 | qte_1788921603531086000_18de2753.zip | FAIL，位于 S10 绿区阶段之后；该 FAIL 原图已出现蓝黄区，不能描述成“绿区内按错” |

4 个 FAIL 不能直接归为反馈观察器故障，也不能仅通过扩大归属窗口解释。
第 10 次输入后来确认 CRITICAL，说明这一轮仍发生后续有效反馈，但不能反证前面的机制已正确处理。

## 后续评估顺序

1. 优先评估 S01/S02/S10 的蓝绿混合与绿区阶段：确认玩家应采取的动作、切换依据、进入和退出条件；原序列不足的部分明确留待补样。
2. 评估紫色图示与光标遮挡、目标区变化的关系；把外观识别、允许输入区域和输入模式分别确认，避免颜色触发盲按。
3. 结合 S06/S07 和 R39-U1/R39-F1 复核第 9 次输入的决策、观察窗口及实际反馈，不改写原始账本。
4. 对 R39-F2–F4 逐段确认未输入原因：是否没有合法重合、识别丢失或需要特殊操作；尚不把它们统一归因于时延。
5. 规则确认后提取真实正负例、完成离线回归，再用源码进行对应机制实测；只有覆盖到该机制才标记实机验证通过。

每项状态按“已有证据 → 规则确认 → 实现与离线回归 → 实机覆盖”推进。
本案例当前处于已有证据阶段；冰冻仍没有在本轮确认，绿色维持控制、隐形预测及交叠解算也没有因本次归档而完成。
