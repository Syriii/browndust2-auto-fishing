# 实机问题分类与逐项处理

依据：2026-09-08 21:46:12 / 21:53:02 两次实测。64 份异常相关包是多层取证，不代表 64 个独立故障。60 份 QTE 包中 6 份明确 MISS/FAIL，54 份按键归属未确认。完整本机索引位于 .local/maintenance/live-error-analysis/report.md。

## 2026-09-09 暂缓机制、优先处理普通流程

按用户要求，R39 特殊机制身份及失败因果暂缓讨论，原始案例和编号保持不变。
本轮修复结算预算被截为 4 秒、动作沿用旧页面以及取证容量不足时直接淘汰上下文的问题，
并提供本机基础等待校准。已确认待机页可有界续钓，连续结果 unknown 上限默认两轮；
真正未知页面和 QTE 整轮控制超时仍停止。实现、验证和限制见[当前状态](status.md)。
这些改动不表示 R39-U1、四个 FAIL、旧 26 个反馈超时或所有特殊机制根因已解决。

## 2026-09-09 10:39 自定义时序验证的新样本

本轮已建立[独立案例记录](cases/2026-09-09-b3e84021.md)：逐项列出全部 11 次场景事件、
R39-U1 未确认与 R39-F1–F4 四个 FAIL，编号不混入旧 60 包。
原图确认存在蓝绿混合、仅绿区、仅黄区和紫色图示阶段；具体机制与操作规则仍待逐项确认。
10 个原 ZIP、日志和摘要已复制至诊断轮转之外，并制作包含 114 张提取原图的本机图册。

仅测试进程使用 80ms 按住、160ms 松开后等待，原个人配置不变。10 次输入包含 7 次暴击、
2 次 HIT、1 次 unknown，另有 4 次无关联输入 FAIL；画面含紫色障碍和较大剩余距离。
按键 9 的未确认与后续 FAIL 暂未确定根因，不能从一轮不同场景就认定缩短时延有益或有害。
最终画面没有奖励，结算在 19 次检查后按保护逻辑停止，unknown 保留；禁止把测试记成成功捕获。
完整证据在本机 `.local/maintenance/live-review-20260909-103930/`，首末结算图、失败包和场景时间线已保存。
该轮验证了自定义参数进入正式输入路径及未确认结算的停止分支；游戏效果仍需同类场景对照。

## 2026-09-09 10:23 观察与取证优化

在保持冰冻、绿色维持区待采样的前提下，修复普通新帧可能挤掉异常旧帧的问题，
优先保存首次候选和结束帧，明确记录淘汰类型及事件原图是否还在包中；减少相邻亮线多指针候选，
保留原始位置并新增可信位移/反向记录。反馈对完全相同的上一帧复用识别结果，时间与归属照常更新。

204 项离线回归通过；80 秒源码测试 28 键均确认暴击、三次确认捕获。
本次真正触发备用暴击识别；第三轮八个事件的时刻原图全部保留，仅淘汰 11 张普通时间线，
红/紫色候选身份仍不直接认定。时延、停止与证据核对见 [当前状态](status.md)。

全部旧 60 包重放后，原 26 个待归因反馈超时包在按键后的 0.75 秒内仍无可识别字样；
新识别出的部分 CRITICAL 位于该范围外，不能用于补写成功。U01/U04 仍是旧 FAIL，U49/U50 为 HIT 漏检。
逐项结果保留在本机 `.local/maintenance/observation-optimization-20260909/history-60.md` 和 JSON，
包含每帧识别及原时间，原始账本和 ZIP 未修改。F01 的具体根因仍缺证据。

## 2026-09-09 09:51 源码初测的新证据

运行 `bdf2f13` 约 79.97 秒；三轮共 21 次 QTE 按键，20 次归属暴击、1 次 unknown，未观察到 MISS/FAIL。
轮内最短输入间隔 797ms，未再出现旧 328–329ms 重复触发模式；特殊机制未完整覆盖。
详见 [当前状态](status.md)。

| 编号 | 新证据 | 状态与后续处理 |
| --- | --- | --- |
| SET-02 | 93166be8-001 的 `settlement.png` 显示结算过渡/加载画面，没有奖励面板；程序单次结算读取后保留 unknown | 已实现：既有等待预算内复查，最多 4 秒、21 次，保存首末帧；仍无面板则停止、不点击。过渡/超时/取消原图回归通过；复测两次均首帧就绪，重试分支尚未实机触发。旧捕获不补写 |
| OBS-03 | 93166be8-003 第 2 键后 +219ms 原图可见被强光覆盖的 CRITICAL；有效观察 17 帧、最大采样间隔 78ms、最高字形匹配 0.737，小于 0.80 | 已实现：白字识别不足时，原暴击模板的边缘匹配至少 0.80 且同位置灰度匹配至少 0.65 才确认。新旧真实正负例通过；548 图新增 23 张可见暴击，旧 unknown 不改写。复测 26 键均确认，不据此断言强光分支实机验收完成 |

本机证据目录 `.local/maintenance/live-review-20260909-095114/`，上述原图另提取为
`93166be8-001-settlement.png`、`unconfirmed-frame_02.png`，对应原 ZIP 保留。
第二、三轮奖励面板已分别确认蓝龙海神 3.1cm 与斗鱼 6.6cm。
两份多指针候选包只证明外观取证触发，相邻候选仅差 4/6 像素，不能据此宣称真正分身机制实测通过。

10:05:51 修复后复测确认两次捕获，第三轮限时主动中断；没有新反馈未确认包。
保存紫色外观与无可信光标候选，第二轮有界缓存淘汰 23 帧，保留 71 帧；事件记录未丢失。
本次新问题的修复通过 199 项离线测试与静态检查，实机覆盖边界和完整证据见 [当前状态](status.md)。
下文 26 份旧超时与帧标签是当时识别器的历史分类；新字形回放改善不自动恢复旧按键归属。

## 2026-09-09 超时处置与反馈漏检修复（实现时仅离线）

整轮 QTE 达到 `longest_keep_time`（默认 35 秒）后，旧实现直接返回主循环，可能在游戏仍处于 QTE 时重抛。
两套策略现在走同一只读检查：保存当前控制原图，检查倒计时；没有倒计时且结算观察器可用时，
再用现有面板/奖励识别核对现场。区分 `qte_active`、`settlement_visible`、`capture_unavailable`、
`unrecognized_page`。随后抛出专用超时异常、保存整轮证据、停止任务并释放输入，**不自动点击或开始下一轮**。
本次没有实现未知页面的自动恢复；看到结算也只记录，用户确认页面后重新开始。
超时与用户主动中断分开，不能把超时一律当作逃脱；已经确认的鱼获不因控制超时被改写。

固定图册原 30 份反馈等待超时，按实际原图再分组：

| 数量 | 编号 | 发现与处理 |
| --- | --- | --- |
| 2 | U49、U50 | 原图可见 HIT，旧反馈模板仅约 0.66 分；补充 945×532 的两种实际字形后识别约 0.997/0.998。仅修复文字识别，不补写按键归属或破冰成功 |
| 2 | U01、U04 | 窗口内可识别的 FAIL 在按键前已经存在，不能当作新反馈；U04 较晚另有 CRITICAL，也不能无条件归给已超时按键 |
| 26 | U02、U03、U07–U15、U18、U33–U41、U44、U51–U54 | 已保存窗口帧中没有被识别的反馈字样；不能区分游戏未显示、短暂漏样、遮挡或仍有未覆盖字形，不冒充已解决 |

完整逐帧分数与相对按键时间在本机 `.local/maintenance/feedback-timeout-review-20260909.json`。
原 ZIP 的八帧为筛选后的稀疏证据，不能用这些帧的间隔断言当时采集线程断档。
480 张历史观察原图重新识别，仅新增三张 HIT 识别（其中 U49-frame_07 与 U50-frame_03 是同一时刻），
其他帧标签未变化；原始 unknown 不改写。阈值仍为 0.80，类别差值仍为 0.12，没有放宽阈值。

新记录保留 0.75 秒反馈归属窗口，新增采样数量、最大间隔、匹配分数与未确认类别：
无观察帧、观察断档、无识别文字、旧反馈未更新、被下一输入取代、反馈归属歧义、
观察线程失败、按键记录丢失和 QTE 结束。它们是观测证据分类，不是游戏机制根因。
观察线程异常会明确结算待确认按键，后续输入记录不会伪装成成功；超时不会触发补按或延长归属窗口。

193 项离线测试、Ruff、125 个文件格式检查及 67 模块架构检查通过；覆盖两套策略超时、
不同页面分支、缓存落盘、取消、异常、停止传播/释放输入、反馈分类及实际 HIT/背景/其他反馈正负例。
未实测、构建或部署；这不代表 30 份历史超时根因全部解决。

## 2026-09-09 看图反馈后的修复（未实测）

- F05/F06：用户确认为分身，最亮光标为真。正式控制由白像素列计数改为窄竖线候选的亮度排序；清晰观察图定位 x=170/44，旧决策帧缺少可信亮候选时仍不按。不是把“旧检测器没找到”解释为画面没有光标。
- U07：用户确认只剩蓝区时命中蓝区即可。连续原图验证 x=113 在蓝区外不按，下一帧 x=148 进入蓝区后触发一次。机制明确不改变该次按键原始反馈超时的 unknown 归属。
- F01–F04：用户确认为未命中有效区。向前回放，F02/F03/F04 分别能找到旧 U03/U07/U09 序列的蓝区机会；F01 短片没有覆盖可命中瞬间，不能仅凭现有片段确认是检测漏失还是时序问题，也不能宣布其根因已修复。
- 54 份 unknown 保留原分类，不因同类画面出现分身就统一改成分身导致的失败。特殊机制与反馈归属是独立字段。
- 场景取证已接入正式观察器：即使无输入、无 MISS 或最终成功，也保留特殊候选及整轮低频原图。固定 60 包回放生成 33 份候选包、27 份普通包，全部 ZIP/图像完整。候选颜色不派发破冰等输入。

185 项离线回归及静态/架构检查通过，未控制游戏或更新 EXE。真正冰冻、绿色维持、隐形及候选交叠仍待专门序列补齐；下文保留此前实测阶段的结论和日期。

| 编号 | 类别 | 证据与判断 | 处理及验收 |
| --- | --- | --- | --- |
| QTE-01 | 已修复：无光标盲按 | a3f43d48-003 的按键 5、9 在光标掩膜为 0 时仍走 no_cursor_fallback，随后记录 MISS | 已删除该输入分支；两份真实原图回归，22:16:08 限时实测确认捕获两条鱼。不是声明隐形或分身指针已支持 |
| QTE-02 | 已修复：持续重合重复触发 | 40 条未确认记录距离前一次按键约 328–329ms；控制对同一重合状态可重复按键。不能认定全部 unknown 均由此引起 | 两套策略共用 TargetEntryTrigger，同一次重合只触发一次，确认离开或切换黄/蓝目标后再触发；丢帧、无光标、无目标不重新放行。22:18:50 实测确认捕获一条鱼 |
| QTE-03 | 已实现回归：黄区消失后未回退 | 4 次 FAIL 的临近帧没有蓝区重合；继续向前追溯，47843.171/312/437 的连续原图确认有蓝无黄、指针经过蓝区，而旧普通策略不按键 | 连续两帧有蓝无黄才允许蓝区回退；保留黄色优先，空白/灰色/绿色/无光标不作为蓝区。真实正负原图及序列回归通过，最终实机验收见下 |
| QTE-04 | 已修复危险规则；冰冻支持待补 | 初始仅两次少量红像素，不能判断；22:18:50 的 f43f14e2-001 明确出现红色牙齿图示，旧规则触发 19 次破冰尝试 | 删除“红像素即冰冻”的无条件按键，光标附近有红色遮挡时不按，不用该状态重新放行；三份真实原图回归。真正的冰冻三次输入与解除确认仍未支持 |
| OBS-01 | 观察不确定性 | 30 次反馈超时、13 次下一次按键前无反馈、10 次连续归属歧义、1 次停止前无反馈 | 先减少 QTE-01/02 的多余输入；保留 unknown，不延长归属窗口强行匹配，不计作失败率 |
| OBS-02 | 特殊图示下的未确认反馈 | 最终 b7dc27ef-001 按键 7、b7dc27ef-002 按键 8 的决策原图均有紫色图示，未匹配到可归属反馈文字 | 与普通误按分开，保留 unknown。图示和按键同时出现不能证明紫色机制已解除；需要后续连续状态验证，不能补标 HIT |
| SET-01 | 已修复识别缺陷 | 945×532 下灰字关闭模板被亮白阈值清空，匹配分数恒为 1 | 已在 436efcd 修复，保留真实正负例和常量模板回归 |
| STOP-01 | 预期停止记录 | 两段测试第三轮均主动停止，保存 interrupted | 保留中断证据；验证停止后无新操作输入，不改标逃脱 |
| CAST-01 | 已恢复的游戏状态 | 当前位置无法抛竿，移动/点击/重抛后进入 QTE | 已实测恢复；保留一次恢复限制，不作新故障重复修复 |

按 QTE-01 → QTE-02 → QTE-03/04 顺序验证，每步使用正式源码和有界诊断。真正冰冻和紫色图示的效果等证据不足项不能凭猜测写入机制常量。不能因基础启停、取证测试通过，就宣称所有钓鱼机制已覆盖。

## 证据追溯与边界

原图回归集中于 [qte_control](../../tests/fixtures/qte_control/README.md)。把 FAIL 前一瞬间的蓝区缺失与早期可按窗口分开分析；最右侧亮线曾被怀疑是边框，但连续帧显示它在移动，该假设已排除，未加入边缘裁剪。

QTE-04 的防护只依据颜色和局部位置避开遮挡，不是完整的咬人技能分类器。红色内容不能证明冰冻，已移除此前未经验证的自动破冰尝试。冰冻、绿色长按、隐形/分身指针及技能叠加需要真实正负序列与专门规则；不得据此宣称所有机制已解决。

新增规则不改变输入驱动的按下/松开间隔及现有循环等待，不新增输入排队、同步日志或写图。蓝区确认通过原循环的两次有效观测完成，不另行阻塞等待；局部规则速度不代表游戏端到端延迟。

## 最终验证（2026-09-08 22:27:53）

172 项离线回归、Ruff、118 个文件格式检查和 64 模块架构检查通过。
两次最终实测尝试分别因初始化失焦和自动聚焦失败被保护中止，均未进入 QTE；用户恢复游戏并回复可以验证后重试。

最终源码运行 80.109 秒，确认捕获狮子鱼 32cm、31.8cm；第三轮按测试上限主动停止。
27 次 QTE 按键，25 次可归属暴击，2 次 unknown；游戏反馈 MISS/FAIL 为 0。
其中普通策略的蓝区回退执行 1 次，该次为 OBS-02，**未取得可归属蓝区命中反馈**。
本轮没有再次触发红色遮挡，QTE-04 的图示负例防护由原图回归覆盖，真正冰冻恢复尚未验收。

停止后非释放操作 0，85 次系统输入调用全部接受，约 94ms 工作线程退出，无按键保持或相关线程残留；取证提交局部耗时 p95 0.0300ms、max 0.0395ms（27 次），不代表端到端输入时延。
本轮 5 个 ZIP、25 张原图、54 张掩膜通过 ZIP、解码和同帧掩膜核对，无残留临时文件。
完整证据在本机 `.local/maintenance/live-review-20260908-222753`。

本次可以关闭已确认的危险输入分支和覆盖缺口修复；OBS-02、真正冰冻、隐形/分身、绿色长按与机制叠加仍为开放事项。源码提交不意味着部署 EXE 已更新。
