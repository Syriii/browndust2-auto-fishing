# 当前开发状态

[文档目录](../README.md) · [更新记录](../../CHANGELOG.md) · [验证计划](validation-plan.md)

## 0.4.1 桌面视觉对齐（2026-09-14）

按第四版原型调整实际 Tk 界面的字体、标题、图标导航、圆角分区和鱼卡；筛选标签、行内详情和设置表单统一排版。目标页原地选鱼，保存／取消返回待办。图卡支持键盘操作、刷新后焦点和鼠标移出取消，图片缓存有界。

626 项回归、Ruff、128 模块架构、设置与图鉴冒烟、真实历史图片展示，以及最小窗口和模拟 125%／150% 缩放截图通过。模拟缩放不代替跨显示器 DPI 实测。

本地 EXE 已归位 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，临时版本目录已移除。1307 项文件、128 个内嵌源码模块、84 张鱼图、更新恢复、实际 EXE 图鉴与两次正常启动／退出检查通过，487 个原有用户文件保留。GitHub 发布另行核验，上一正式版记录见下节；外层 deployment 未覆盖。详见[0.4.1 验收](cases/2026-09-14-release-0.4.1.md)。

## 0.4.0 已正式发布（2026-09-14）

[v0.4.0 Release](https://github.com/Syriii/browndust2-auto-fishing/releases/tag/v0.4.0) 的 Windows ZIP 与 SHA-256 已上传；[发布构建](https://github.com/Syriii/browndust2-auto-fishing/actions/runs/34834271318) 全部成功。最终源码 626 项本地回归和 GitHub CI 通过，图鉴、设置、目标、鱼获及相关文档已集成。

官方 ZIP 已通过程序真实下载并验证 SHA-256、1321 项文件及 84 张鱼图。用官方包的 Python 3.12.4 编译本地源码，126 个内嵌模块完整比对通过，仅归一化构建路径，未忽略指令、常量、异常表或调试行号表。官方 EXE 图鉴预览、正常模式两次启动／退出、配置与独立 MAX／MIN 待办保留通过；0.3.9、0.3.12 可发现新版，0.4.0 无重复更新。

固定本地入口仍为 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`。本机与云端分别构建，程序文件数和哈希不同，业务源码与鱼图已核对；原配置与记录保留，外层 deployment 未覆盖。详见[完整验收与两套哈希](cases/2026-09-14-release-0.4.0.md)。完整游戏效果、MIN／彩色三级成功页和旧审查问题仍按下节保留边界。

## 0.4.0 主界面、目标与鱼获（2026-09-14）

已实现第四版交互：单窗口五个页面、84 张离线鱼图、简繁搜索与筛选、图标切换网格／列表、行内详情、尺寸条件多选和带图分类历史。目标按任意／MAX／MIN 独立完成，全部完成后停止；跨岛及昼夜等待使用实际地点与游戏时钟，旧记录不抵扣新任务。正常和恢复中迟到的确认奖励先保存，写入失败保留结算页并停止。

626 项完整回归通过（89.285 秒），主界面／图鉴／更新窗口检查、Ruff、245 文件格式、126 模块架构和依赖检查通过；追加目标写入失败的草稿保留检查。0.4.0 已归位固定 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，临时版本目录已移除。126 个模块与源码编译结果一致，1307 项包文件、84 张鱼图、ZIP、实际 EXE 图鉴预览及更新中断恢复核验通过。485 个旧用户文件保留，3 个候选运行文件另行备份；该本机交付阶段尚未更新 GitHub；后续正式发布见顶部，外层 deployment 仍未更新。详见[本版验收](cases/2026-09-14-release-0.4.0.md)。

补充 18 项名称及鱼获定向回归通过，六张独立 MAX 原图均重新执行 OCR 并完成对应目标；罗汉鱼／花羅漢、蝴蝶鱼／蝶魚映射已按原图修正。

历史截图确认 6 轮红色 `Maximum Size`、绿色一级和蓝色二级奖励；黄色 `New Record` 单独保存。MIN 与彩色三级成功原图未找到，不按鱼种推算等级。[截图审查范围与证据](catch-markers-2026-09-14.md)。完整跨岛游戏效果、冰冻／绿色起按端和原八项审查问题保留原状态，不作为等待长期监测的打包前置条件。

## 0.3.12 图鉴浏览调整（2026-09-14）

图鉴取消翻页按钮，改为滚轮连续浏览；左右区域独立滚动，详情字段按列对齐，机制说明改为简短的游戏描述，来源和程序范围放入“资料说明”。小窗口下详情也可完整滚动查看。

608 项回归、图鉴及主界面检查、代码规范、117 模块架构、实际 EXE 图鉴预览和 1305 项包文件／更新恢复验证通过。0.3.12 已归位固定 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，临时版本目录移除，原配置与记录保留。详见[本版验收](cases/2026-09-14-release-0.3.12.md)。GitHub 和外层 deployment 未更新。

## 0.3.11 内置图鉴交付（2026-09-14）

只读图鉴已集成到 EXE：左侧“鱼种图鉴”支持六岛 84 种离线鱼图、简繁别名搜索、钓场／昼夜／稀有度筛选及机制详情。只显示鱼图区域，未接入用户捕获进度或目标任务。

608 项完整回归、主界面／图鉴／更新窗口、117 模块架构、代码规范和依赖检查通过；实际 EXE 图鉴打开、图片加载和正常退出通过，1305 项包文件及更新助手恢复核验通过。新版已归位固定 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，临时版本目录移除，原配置、日志、截图保留并归档。GitHub 与外层 deployment 未更新。详见[验收记录](cases/2026-09-14-release-0.3.11.md)。

## 0.3.10 本机测试交付（2026-09-14）

用户要求持续测试至 EXE 交付，需要长期监测的机制效果不作为本轮完成门槛。该阶段源码与本机测试包版本为 0.3.10，包含下述攻略目录、机制共用、墙体误检修复、墙体及冰晶候选取证；鱼图多选和成就目标仍是方案。最终验证、准确启动路径和哈希见[本机验收记录](cases/2026-09-14-release-0.3.10.md)。

0.3.10 已归位到固定 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，ZIP 和校验值位于 dist 根目录；临时 `release-0.3.10/` 已移除。两个助手经正常关闭后完成整理，1220 项程序文件及 ZIP 哈希复核通过；482 个旧用户文件保留，5 个候选运行文件另行备份，新旧日志分别保存。旧包和两边数据完整归档到外层 `archive/builds/20260914-before-0.3.10-fixed-dist/`。外层 `deployment/` 与 GitHub 正式 Release 未更新。

实际 EXE 预览暴露鼠标位置读取被系统拒绝时初始化失败，已补自身窗口显示器回退并增加正反回归。长期游戏反馈、原审查八项问题继续按原记录保留，不因构建而标记已解决。

## 攻略资料与机制目录（2026-09-14，现已纳入 0.3.10）

以下按实现阶段保留验证证据；其中“未构建／未更新 EXE”仅描述当时状态，当前交付以顶部验收记录为准。

独立墙体取证补齐：控制与观察通过 `BlockerDetector.from_config` 共用配置缩放和纯识别规则，观察器复用已有帧生成 `wall_candidate`、`wall_rects` 与出现／消失／未知生命周期，场景包保存 `wall_detector` 参数。无需按键或失败即可留证，消失不计作解除。601 项完整离线回归通过（87.1 秒），覆盖真实攻略墙图合成时间线、无 QTE 负例及自定义配置一致性；Ruff、227 文件格式和 115 模块架构通过。未增加截图、输入或游戏实测，未更新 EXE。

机制复用：墙体检测、同帧遮挡合并、机制仲裁及普通目标入口均已移入 `BaseQTEStrategy`，六岛共用，不按鱼名或攻略预期启停；地点子类只保留已有黄条面积阈值。墙体同时约束普通目标、泡泡和绿色持有，首次／组合墙体控制图进入正常取证。补原始像素支持检查，修复 Y05／Y39 黄条纹理及 GameKee 压缩光标被闭运算补成墙的问题；保留两份攻略的静态墙体正例。598 项完整离线回归通过（90.1 秒），新增覆盖全部六岛入口的多墙、泡泡与绿色持有组合。未进行游戏实测或构建，完整碰撞跟踪仍待验证。

复核通过 Ruff、227 文件格式、115 模块架构及图鉴文档一致性。钓鱼包 42 文件质量扫描 79.70/100，工具告警经人工核对，详细报告保存在 `.local/maintenance/guide-catalogue-20260914/shared-mechanisms-quality-review.md`。墙检测含光标色罩在 19×244 原图上的局部均值约 0.108ms、p95 0.165–0.178ms，不含游戏端延迟。

验证安排更新：用户明确无法专门演示冰冻和绿条，两项改为日常使用中自然遇到后再反馈。优先复核正常运行保存的场景、反馈与日志，不再等待或要求手动录像；自动破冰及绿色起按识别继续标记未完成，不以暂缓验收视为已解决。其余工作不以这两项专门演示为前置条件。

图鉴目标方案补充确认：首版支持跨岛多选与昼夜等待，直到全部目标及尺寸条件完成；鱼获首页优先展示首次捕获、彩色和 Max／Min 成就，可展开完整历史。已写入[方案与验收示例](../design/fish-targets.md)，本次只确认需求，尚未接入产品 UI 或目标任务。

手动机制采样准备：新增只读 `record_manual_mechanisms.py`，保存有界局部原图、抓取前后空格状态、丢帧及停止原因。6 项模拟回归覆盖失焦帧丢弃、启动保护、PNG 往返、写盘失败、预算与非法时长。尚未运行游戏采样；冰冻完整计数变化与绿色正确起按／松开连续证据仍缺，未因此启用自动解除。

后续冰冻观察：新增 `freeze_candidates.py`，按数字描边和周边冰蓝外观生成 1/2/3 候选，接入场景包和外观生命周期；不接控制输入。独立巴哈计数 3 与 11 类其他图例负例通过，1/2 仍仅有模板同源图。588 项完整回归通过（81.6 秒），随后收缩候选搜索范围，19 项受影响专项通过；115 模块架构、225 文件格式及资料一致性检查通过。局部观察匹配均值约 0.59–0.74ms，蓝条早退约 0.019ms，仅本机离线基准，不含截图、OCR、游戏输入。完整破冰控制仍未完成。

已建立[钓鱼资料库](../reference/fishing/README.md)：两份来源、84 种鱼、繁简别名、昼夜、稀有度、鱼图来源及 23 张机制图例索引。14 项机制按来源并集登记，场景证据附可能机制、未映射线索和未确认身份；多墙可达区间现已共用于六岛。

583 项完整离线回归通过（92.4 秒），Ruff、223 文件格式、114 模块架构与图鉴数据／文档一致性通过。质量复核 83.37/100。真实墙体／指针负例及合成多墙回归已覆盖；未进行本轮游戏实测。鱼获记录、图片多选和 Max／Min 目标见[讨论方案](../design/fish-targets.md)，尚未接入 UI；冰冻、绿色起按端等完整解除仍待验证。

上述改动最初基于 0.3.9 源码完成，现随 0.3.10 本机包交付。此前 0.3.9 正式 Release 不包含本轮资料库、多墙和取证索引改动；旧八项审查问题未在本轮关闭。

## 已发布产物

此前源码、本机 dist 交付及正式 Release 为 **0.3.9**，包含黄条实体分组和绿色范围分离修复。[v0.3.9 Release](https://github.com/Syriii/browndust2-auto-fishing/releases/tag/v0.3.9) 的 Windows ZIP 与 SHA-256 已上传，发布构建通过。完整交付与验证边界见[本版验收记录](cases/2026-09-14-release-0.3.9.md)。外层 deployment 未更新。逐轮测试、旧候选和迁移记录见[阶段档案](../history/development-2026-09.md)；以下历史小节中的“未打包”是当时状态，以最新验收记录为准。

## 0.3.9 发布验证（2026-09-14）

574 项完整回归（101 秒）、Ruff、112 模块架构、依赖一致性、隐藏主界面和更新窗口通过。新包 1215 项清单、ZIP、EXE 预览及真实更新助手隔离替换/中断恢复通过；包内静态核验已确认黄条实体分组模块和绿色范围分离分支存在。没有进行本版游戏实测，审查的 8 项问题仍待修复，外层 deployment 未更新。

## 全量代码审查（2026-09-14）

历史问题是否解决及修复必要性见[逐项复核](issue-status-2026-09-14.md)。审查时的 0.3.8 EXE 缺少最新黄条实体分组与绿色范围分离修复；本次 0.3.9 已入包并静态验证，实机效果仍待确认。

574 项离线回归、Ruff、112 模块架构、隐藏主界面/更新窗口及依赖一致性检查通过；业务包质量工具评分 83.25/100。人工路径核查与隔离复现确认 2 项 P1、6 项 P2：手动清包缺前台保护、出售过程缺页面确认、保留目录大小写、启动 150302 缺 OCR、INI 保存语法、整数校验、更新结果缓存及输入释放失败处理。**本轮仅审查和更新文档，这 8 项尚未修复。** 完整位置、复现和优先级见[审查报告](code-review-2026-09-14.md)。未操作游戏、构建或更新任何 EXE。

## 困难轮次的机制协同优化（2026-09-14，源码未打包）

第024轮真实控制帧暴露绿色候选将整条普通目标判断暂停的问题。已改为在绿色完整范围及安全边距外继续
判断黄蓝目标，保留未知身份、长按独占、其他障碍和入区去重。新增首张控制帧与绿色跨度记录。
原帧光标尚未进入黄条，回归明确区分原图不误按与合成光标入区，不宣称该帧或特定鱼种已实机命中。

574项完整离线回归、Ruff、218文件格式及112模块架构检查通过；质量复核83.25/100。
包含前一项黄条分组修复；未更新EXE、未操作游戏。绿色起按端、隐形/完全遮挡光标和实际机制解除仍待验证。
详见[绿色与普通目标分离处理](cases/2026-09-14-green-separation.md)。

## 黄条首次经过误拒绝修复（2026-09-14，源码未打包）

已重做黄条光标孔洞的实体分组：孔内孤立像素不再截断两侧支持；重合与中心偏好使用同次结果。
两帧完整边界确认后，可在 180ms 内辅助弱边缘，当前两侧仍须存在黄色；变化、遮挡、缺帧和过期失效。
新增每轮最多三份拒绝按键的控制帧和去重/跨度依据，不增加 QTE 截图、OCR 或等待。

41 帧候选中 32 帧单帧恢复有效判断，另 1 帧通过真实前序确认；其余 8 帧缺乏足够证据，继续保守处理。
50 项专项、567 项完整离线回归、Ruff/216 文件格式/112 模块架构通过，质量评分 83.27/100。
未操作游戏或更新 EXE；0.3.8 不含此修复。详见[黄条碎片复核](cases/2026-09-14-yellow-fragments.md)，报告在 `.local/maintenance/yellow-fix-20260914/`。

## 0.3.8 本机交付：150302 叠层提示修复（2026-09-13）

0.3.7 的 `55ea1917-080` 轮，上层“已耗尽体力。error:150302”被误归为鱼获，关闭后下层鱼获因一次性权限已消耗而滞留约四分钟。已独立识别与核对 150302，按层分别关闭；模板与完整错误码双重确认，OCR 后复核页面，未生效按 2/30 秒冷却重试。150402 退出重进与普通鱼获防重复保持独立。

42 项专项和 560 项完整离线回归、Ruff、214 文件格式、111 模块架构检查通过；质量分析 83.42/100。真实截图和模拟输入验证通过。现已构建并更新固定 dist 入口到 0.3.8：1215 项程序文件、完整 ZIP 与校验值、EXE 预览启动退出、更新助手隔离替换及中断恢复通过。额外核对新模块已包含在 EXE、150302 模板与源码哈希一致。原配置、日志、截图等 445 个用户文件逐项保留并核对哈希；旧包和原数据备份到 archive/builds/20260913-before-0.3.8。构建与交付报告位于 .local/maintenance/release-0.3.8-20260913/。未操作游戏、未发布 GitHub，外层 deployment 未更改。详见[叠层提示复核](cases/2026-09-13-layered-exhausted-notice.md)，证据与报告在 `.local/maintenance/stuck-20260913-1743/`。

## 0.3.7 本机交付（2026-09-13）

已用最新源码重新构建并更新固定入口 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，包含夜间升级识别与关闭重试、普通黄条光晕修补、计时颜色短暂失效接续、泡泡残影局部避让及组合保护。此前仅含升级页修复的候选已归档，当前包包含全部后续修改。

552 项完整离线回归、Ruff、212 文件格式、110 模块架构、界面与更新窗口检查通过。质量分析 83.42/100，人工复核未发现新增阻断缺陷，QTE 主循环的复杂度仍是维护负担。1214 项程序文件、ZIP 与校验值、EXE 预览启动退出、真实更新助手隔离替换和中断恢复均通过。

旧交付及用户数据归档到 `archive/builds/20260913-before-0.3.7`；配置、日志、截图等 433 个用户文件已还原并逐项核对哈希。旧候选归档到 `archive/builds/20260913-obsolete-0.3.7-level-up-only`。外层 deployment 未更改，未操作游戏、未发布 GitHub；完全遮挡光标与机制实际消除效果仍待长期实测。

报告见 `.local/maintenance/package-review-20260913/`；问题依据见[升级页复核](cases/2026-09-13-level-up-retry.md)、[普通 QTE 复核](cases/2026-09-13-ordinary-qte-targets.md)、[连续性修复](cases/2026-09-13-qte-continuity.md)。这些案例中的未构建说明是历史状态，以本次交付记录为准。

## 0.3.6 本机交付（2026-09-13）

已重新构建并更新固定入口 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，包含更新检查限流修复、黄条实体边界、拉竿后的页面确认、奖励数量漏读、多个泡泡独立去重、绿色与不相交泡泡的协调处理、真实贝壳避让、挡板内光标拒按及组合代表帧取证。

536 项完整离线回归、主界面与更新窗口检查通过。1212 项程序文件及 ZIP 哈希、真实更新助手替换和中断恢复、EXE 预览启动退出通过。新增两份贝壳模板已随包提供。保留配置、数据、日志、截图、缓存共 394 个文件并逐项核对哈希；旧交付归档至 `archive/builds/20260913-before-0.3.6`，仅含更新修复的旧候选归档至 `archive/builds/20260913-obsolete-0.3.6-update-only`。

验证记录在 `.local/maintenance/release-0.3.6-20260913/`。未操作游戏，未发布 GitHub；完全遮挡、绿色起按端、真实冰冻与特殊组合的游戏效果仍待用户持续实测。详见[组合处理](cases/2026-09-12-mechanism-combinations.md)、[晚间复核](cases/2026-09-12-evening-qte.md)、[更新限流修复](cases/2026-09-12-update-rate-limit.md)。

## 0.3.5 本机交付（2026-09-12）

包含天空岛目录与白天升级页关闭修复。503 项离线回归、界面/更新窗口、Ruff/格式/架构、1210 项程序文件、EXE 预览与更新助手中断恢复验证通过。固定 dist 入口已更新，316 个用户文件逐项哈希相同，旧包及原始数据归档至 archive/builds/20260912-before-0.3.5。未操作游戏，未发布 GitHub；天空岛专属机制尚待真实样本。

## 白天升级页关闭补充（已纳入 0.3.5）

用户 2026-09-12 原图为 1192×666 的 26→27 升级页，底部有点击画面关闭。原关闭提示可识别，但属性标签分数 0.8683 低于 0.88，错误归为 result；本轮已关闭 result 后因此被防重复点击规则挡住。新增白天标题和标签模板，保留三项交叉确认及原阈值。真实原图、两个缩放适配、缺少标题/标签/关闭字样，以及已关闭鱼获后关闭新升级页并确认待机均通过回放；原有独立升级样本、停止和禁止重复点击测试仍通过。63 项专项与 503 项完整离线回归通过，Ruff/格式/架构通过。原图及来源记录在 tests/fixtures/catch_result/README.md。天空岛与此项修复均已包含在 0.3.5 EXE 中，游戏端待用户实测。

## 六钓场目录补齐（已纳入 0.3.5）

2026-09-12 将天空岛加入统一钓场目录、OCR 别名和策略映射；界面由目录自动生成六项，导航、退出重进、往返刷新接受天空岛。暂用现有通用 QTE 策略，启动日志明确未实测；不自动购买许可证，不宣称专属机制已验证。购买前后真实原图验证拒绝购买与已持证启航。67 项专项回归通过；500 项完整回归中 499 项通过，唯一失败为日志测试使用 object.__new__ 未初始化地点；补齐该测试前置条件后 6 项日志测试通过。Ruff/格式/架构通过。0.3.5 已部署 EXE 包含此次补充，最终 503 项完整回归通过。

## 0.3.4 白天码头恢复修复（2026-09-12）

已更新固定入口 `dist/BD2_AutoFishing/BD2_AutoFishing.exe`，498 项完整离线回归、Ruff/格式/架构、界面/更新窗口及 1208 项包文件检查通过；EXE 预览启动退出正常，更新助手替换和中断恢复通过。用户确认退出后已归档旧包至 `archive/builds/20260912-before-0.3.4/`，保留 300 个用户文件并核对哈希，1208 个程序文件与已验证构建一致。未游戏实测、未发布 GitHub。详情见[故障与修复记录](cases/2026-09-12-dock-retry.md)。

## 0.3.3 最终开发验收与交付（2026-09-12）

补充重进过程中关闭结算面板不能跳过返回原岛屿的边界，并记录从码头/地图开始的未完成重进。495 项完整离线回归、Ruff/格式/架构、UI 启停/设置和更新窗口模拟均通过。最终包 1208 项文件、EXE 启动退出、更新助手隔离替换及中断恢复通过。旧交付及 0.3.2/早期 0.3.3 候选完整归档，固定 dist 已换为最终 0.3.3，原 285 个用户文件逐一哈希相同；外层 deployment 未更改。未游戏实测、未发布 GitHub。

## 等待页中断修复（v0.3.1 之后，源码）

2026-09-12 补充：通用 `FishingReentry` 已接入等待恢复超时和 `FishingStalled`，使用已有真实码头、地图、岛屿及返回确认样本验证。494 项完整离线回归通过，专用/通用恢复失败会间隔重试，保留最近 10 次动作记录。0.3.3 候选包含本次退出重进补充，长时间游戏实测仍待验证。

0.3.2 本机候选已生成至 `dist/release-0.3.2/`，486 项离线回归、EXE 预览及更新助手检查通过；尚未游戏实测、未覆盖原交付或发布 GitHub。

EXE 于 19:17:44 的 64909f88-115 轮因连续未确认计数达到 3 停止，恢复采样尚未执行。原图明确是等待咬钩，不能据此重抛。当前源码删除计数停机规则，已识别等待/QTE/待机按状态接续；运行中识别等待超过预算仍观察，保留最近 300 个恢复样本。等待咬钩及抛竿前的页面观察超时也进入同一恢复入口。尚未进入 QTE 时 3 秒转回页面检查，实际 QTE 控制时序保持原样。详情见 [此次排查](cases/2026-09-11-waiting-interruption.md)。

## 0.3.1 验收（2026-09-11）

源码实测完成码头 → 地图 → 亚特兰蒂斯，以及连续三条捕获、21 次暴击、0 MISS/FAIL；黄条中心偏好实际延后 13 帧。第一次抛竿位置受阻后自动恢复，第四轮由 90 秒测试时限中断。480 项离线回归、界面/更新窗口、Ruff 和架构检查通过。

本机 `dist/BD2_AutoFishing/` 已更新，EXE 预览、1208 项文件与更新助手隔离替换/恢复检查通过，原 263 个个人文件逐一核对保留。旧包完整归档。150402 恢复和罕见组合机制没有在本次实测触发；本机包与 GitHub CI 包分别构建，具体校验值以各自产物为准。详见 [0.3.1 验收记录](cases/2026-09-11-release-0.3.1.md)。

## 区域随机点击与剩余 QTE 异常（2026-09-11，源码已离线验证，未打包）

新增通用 `runtime/click_area.py::random_point` 与受保护的 `infrastructure/windows/input.py::click_in_rect`。默认内缩 20%，支持小区域和多屏负坐标；码头开始、更改、启航、换岛确认使用当前 OCR 按钮文字范围，150402 关闭/返回也已接入。未确认区域不统一偏移；实际点击坐标写入导航记录。没有新增随机等待、鼠标轨迹或 QTE 按键抖动。

M13 的遮挡旁残黄缺少实体竖向支持，原输入已在原图回放中拦截；有效黄条和 M07 分身图没有被一并收紧。FAIL 改为独立游戏事件与未归因证据，保留当前按键等待窗口；后续 HIT/MISS 可继续归属，只有 FAIL 则按键结果未确认。M01/M06/M11 的历史时间线用于验证新规则，不重写旧包，也不宣称机制已成功解除。

479 项完整回归通过，随后规则整理与索引分类补充后 49 项相关测试通过；Ruff 和 105 模块架构检查通过。M02/M03/M05/M07/M08/M09 仍待轨迹、遮挡和输入生效时机证据，M01/M11 机制原因仍待确认；本机 EXE 尚不包含本节改动。

## 普通黄条中心偏好（2026-09-11，源码候选，未打包）

新增 `game/fishing/mechanics/yellow_aim.py`，两套钓点策略共用：只在已有按键许可、目标端点稳定、连续三帧运动可信且下一采样仍有空间时继续跟踪，优先更接近黄条中心的机会。高速、反转、停顿、窄条、邻近障碍或轨迹不明时不强等中心；延后不重新解锁同次按键。保留原膨胀、按住/松开时序，不增加截图、OCR 或 sleep。

M10/M15 的真实前序观察帧与决策图回放会延后原来的边缘按键；M03 速度波动大，仍保留原机会。观察帧并非连续控制帧，且原输入已改变后续游戏画面，不能据回放声称新的输入会暴击。详见[目标判定记录](cases/2026-09-11-qte-target-evidence.md)。旧的强制黄区内部方案实测退化记录继续保留；需要后续源码短测及 EXE 长期验证。

465 项完整回归、Ruff、196 文件格式和 104 模块架构检查通过。新增判断的本机 CPU 测量中位数约 0.08 ms、P95 约 0.16–0.17 ms，不含截图、输入与游戏端时延；未更新 EXE。

## QTE 残色误触发优化（2026-09-11，源码回归通过，未打包）

逐项复核 M01—M15 后，发现 M04/M06/M12/M14 只有 1–4 个普通黄色像素却被膨胀为黄条。源码先清除遮挡，再检查原始黄色支持；四个原按键不再放行，有效黄条保持既有膨胀与输入时序。新增每轮最多一张残色拒绝的控制 ROI，后台编码，不额外截图。

454 项全量回归通过，补充取证后 42 项相关检查再通过；Ruff、193 文件格式、103 模块架构检查通过。剩余 11 份不能据当前证据全部修复，尤其边界时机、分身与按键后 FAIL 归属需要 EXE 长期验证；本次未实测或打包。逐项结论见[目标判定取证记录](cases/2026-09-11-qte-target-evidence.md)。

## 150402 恢复与 QTE 取证（2026-09-11，源码已离线验证，未打包）

下午 EXE 的 179 轮中，最终停止现场为 `error:150402`。用户确认关闭后仍无法钓鱼，需要返回码头重新进入。新增 `game/navigation/reentry.py` 专用状态链：错误码/确认按钮复核 → 关闭 → 钓场背景确认 → Esc → 返回码头确认 → 码头 → 原钓场 → 待机。所有阶段有时间与次数上限，保留窗口和取消保护；普通导航仍不确认返回。447 项全量回归通过；未实测退出重进，现用 0.3.0 EXE 尚不包含此修复。

QTE 失败优先按按键取证，不以鱼获结算截图代替。当前本机图册 `.local/maintenance/qte-review-20260911-afternoon/index.html` 包含 15 份按键未命中、48 份无对应按键 FAIL、37 份未确认，完整原包已复制；早期 12 份已被原 100 包上限轮转，不能伪造补齐。普通未确认后返回待机的空场景在新源码中省略，页面异常、错误恢复与奖励 OCR 证据仍保留。

## 0.3.0 本机包已完成（2026-09-11）

本机 `dist/BD2_AutoFishing/BD2_AutoFishing.exe` 与 `dist/BD2_AutoFishing-windows.zip` 已更新为 0.3.0；原配置、数据、日志和截图共 145 个文件逐一校验保留。旧包完整归档，GitHub 尚未发布本版。

433 项回归、Ruff、187 文件格式与 101 模块架构检查通过。最终 90 秒源码实测确认三轮捕获、共 21 暴击，其中一轮经过位置受阻恢复。新 EXE 预览启动/退出、资源完整性、更新助手替换及中断恢复通过；EXE 本身尚未进行钓鱼实测。

严格黄区内部判定候选经实测出现回退，已撤回，不包含在本包中。黄区时延、组合机制、真实拖图搜索及其他岛屿往返仍待验证。详见[0.3.0 验收记录](cases/2026-09-11-release-0.3.0.md)。以下保留各阶段当时的源码与打包状态，以本节为当前交付结论。

## 自动换点统一导航（2026-09-11 13:10，源码已实测，未打包）

正式 `game/islands/travel.py` 已迁为中转与返回策略，删除固定坐标选岛和盲点确认，两段均用 `VoyageNavigator`；确认返回后再复核待机并抛竿。出发前当前岛名 OCR 漏读不再挡住已确认的更改入口，但地图目标及确认框目的地仍严格核对。OCR 不可用或空读数不触发自动换点。

实测亚特兰蒂斯 → 烟波湖 → 亚特兰蒂斯完整往返成功；随后一次位置受阻恢复后确认捕获锯鲨。第二轮组合外观下结算未确认，保存证据后继续第三轮，第三轮由测试时限停止。不能将本次衔接通过表述为 QTE 全部问题解决。28 帧新机制时间线已保存在本机维护目录。

433 项整套回归、Ruff、186 文件格式、101 模块架构检查通过。完整经过和待验证项见[实测记录](cases/2026-09-11-voyage-navigation.md)。真实拖图搜索、其他岛屿往返、到期触发、组合机制仍待覆盖；未打包。

## 岛内换图与换岛确认（2026-09-11，源码已实测，未打包）

新增两类确认框识别。“前往钓鱼地区”按用户确认的普通换岛语义处理：核对目的地、确认一次、观察到达；返回码头仍不自动确认。`--navigation-only` 支持从待机钓场点击“更改”，到达目标后停止。

12:25 源码完成启航、确认、到达深渊巨口；随后从岛内“更改”进入地图，选择并确认亚特兰蒂斯，12:26:56 到达待机。两次均无抛竿、购买或返回码头动作。425 项整套回归通过，随后抽取到达处理函数、统一诊断样本上限后 24 项导航回归再次通过；Ruff、186 文件格式、101 模块架构检查通过。质量整体 84.76，导航协调 84.55、识别 89.91、确认框识别 96.19。

该阶段拖图搜索、天空岛策略、旧换点迁移仍未完成；旧换点随后已迁移，见上节。证据见[本次实测记录](cases/2026-09-11-voyage-navigation.md)；未更新 EXE。

## 码头与选岛导航（2026-09-11，源码已实测，未打包）

补充航海页面 OCR、圆标定位、许可证与等级独立判断、鱼种解锁读取，并接入 session 启动。实测已跑通码头 → 选岛 → 亚特兰蒂斯启航 → 可抛竿页面；地图实机读取 13 已解锁、2 未解锁鱼种，后续三轮确认捕获并继续钓鱼。首次抛竿曾等待超时，后经待机复核及明确位置受阻处理恢复，此问题仍保留证据。

411 项整套回归通过，后续拆分识别函数与补充拖动测试后 18 项相关回归通过；Ruff、184 文件格式、100 模块架构检查通过。整体质量评分 84.78，导航识别 90.27。实际拖图搜索、天空岛进入、主页到码头仍未覆盖；未更新 EXE。完整证据与边界见[本次实测记录](cases/2026-09-11-voyage-navigation.md)。

## 等待咬钩识别修正（此前源码基线，未打包）

2026-09-11 用户补充抛竿前正、背面截图，水花按钮明确归为 waiting；旧无方向键待机规则已撤回。等待图和背面待机原尺寸回放通过，正面裁切图仍拒识，未放低阈值。
启动/异常恢复可以接续等鱼；普通等待超时先识别页面，不再直接移动重抛。仅当前等待帧的咬钩信号能触发一次拉竿；回到待机后才可重抛。等待超时和未知保留截图。
397 项完整离线测试通过，Ruff、179 文件格式与 98 模块架构检查通过；未实测、未更新 EXE。样本与依据见[页面识别](../design/scene-recognition.md)。
现用 0.2.1 仍包含旧水花待机判断，不能视为已经修正；下文为此前实现的历史基线。

## 0.2.1 之后的源码修复（未打包）

新增[页面识别与启动接续](../design/scene-recognition.md)：启动先确认待机、面板或 QTE，未知页面有界观察并记录；首次抛竿和换点前后都检查待机。
已有 QTE 接续时跳过换点、抛竿和咬钩按键；接续之前的输入不计入统计，接管时已结束则走原计时条消失与结算路径。
`scene.py` 组合纯识别，`startup.py` 管理启动观察与独立证据，`panels.py` 同时拥有关闭提示和升级字形；架构检查约束纯识别模块不导入设备实现。
新增 14 项离线检查，完整 386 项通过，补齐证据 ID 后启动 14 项再次通过；Ruff、178 文件格式与 97 模块架构检查通过。
质量工具 84.81/100，识别模块 92.24、启动模块 89.73；报告 `.local/maintenance/startup-quality-review.md`。
当前 EXE 未覆盖，未做游戏实测；下列启动基线后已补充等待咬钩接续；主页、玩法入口和完整地图／背包页面仍未纳入。

2026-09-10 23:01:12 第 90 轮已捕获神仙鱼，但鱼获关闭后出现等级 25→26 的升级提示，触发旧版同轮单次关闭限制。
现增加 `panels.py` 区分已确认可关闭的鱼获和升级提示；`recovery.py` 按弹窗类型分别关闭，确认全部处理后恢复待机并继续钓鱼。
标题、属性标签与关闭提示共同确认升级，关闭前再次核对类型；同一不变弹窗不重复点击，取消和窗口保护照常生效。
正常结算与异常恢复共用此流程，已有捕获结果保留；`panel_close_history` 和 `panel_before_*.png` 记录每个处理阶段。
新增两张升级原图、一张该轮奖励图用于离线回归；实测与 EXE 更新尚未执行。
372 项离线回归通过，新增 10 项升级场景检查；Ruff、175 个文件格式与 95 模块依赖检查通过。
质量工具整体 84.74/100，升级识别模块 96.97/100；报告与验证日志保存在 `.local/maintenance/interruption-review-20260911/`。

## 发布后的本机修复版 0.2.1（未发布）

针对 18:11:55 超时，新增 QTE 超时/结算观察异常后的有界页面恢复；先结束 QTE 输入和反馈，确认待机后续钓，保留原未确认结果与异常现场。
用户确认的无方向键、水花抛竿按钮布局已纳入识别，要求水花和 SPACE 字形同时匹配且没有可见倒计时。
默认恢复观察 10 秒，沿用连续未确认最多续钓两轮；同轮只累计一次，关闭面板最多一次。
停止/失焦和清理失败不进入恢复，配置错误等非轮次观察异常仍上抛。正常 QTE 逐帧输入路径未增加识别或写盘。

362 项离线回归通过，含此次两张原图及真实 QTE/奖励/加载负例、恢复期间取消、输入清理失败和重复关闭保护；Ruff 与 93 模块架构检查通过。
0.2.1 已构建并更新至本机 `dist/BD2_AutoFishing/`，1200 个清单文件、ZIP 校验、隐藏 Tk 和更新助手隔离替换/恢复通过。
更新前确认旧程序退出，旧整套程序归档至外层 `archive/builds/pre-0.2.1-20260910-221333/`；14 个用户文件逐一核对 SHA-256，配置、日志、截图及缓存均保留。
本次没有 EXE 游戏实测，也未发布 GitHub Release；外层 `deployment/` 未变更。原始上钩候选为何未进入 QTE 仍需触发瞬间证据，未盲调颜色阈值。
本机逐包复核页见 `.local/maintenance/runtime-review-20260910-evening/index.html`，分类见[错误复核](error-triage.md)。

## 0.2.0 历史发布基线

- 发布源码：`1c9675525a4469b0b340c69c28616ceb61fccba5`，由 [PR #1](https://github.com/Syriii/browndust2-auto-fishing/pull/1) 合入 main 后固定标签。
- [Windows 发布构建](https://github.com/Syriii/browndust2-auto-fishing/actions/runs/34446286495) 完成回归、打包和上传。附件为 `BD2_AutoFishing-windows.zip` 及同名 `.zip.sha256`。
- 官方 ZIP 为 92,559,146 字节，版本清单 0.2.0，1,212 个程序文件逐项校验通过。
- ZIP SHA-256：`1913186e5369a04015f33d95d9f3834b0dd1338e3b5499220fef1e7d0bd40207`。

本机 `dist/` 候选与云端正式包分别构建，依赖文件数和哈希可能不同；用户下载和后续验收须记录实际使用的包。发布未覆盖维护者当前使用的 `deployment/`。

发布后 main 可继续合入文档与测试修正，已发布标签和附件保持固定；合并 main 不自动产生新版本。版本规则见[分支约定](branching.md)。

## 当前能力与限制

| 范围 | 已实现 | 仍需完成 |
| --- | --- | --- |
| 桌面 | 任务与日志同屏，单一启停按钮，嵌入设备设置，更新/截图入口 | 不承诺所有 DPI、屏幕及系统组合均已验收 |
| 普通钓鱼 | 抛竿、上钩、QTE、分层结算与持续页面恢复，满包按设置处理 | 清包页面确认、启动 150302、长运行与新增场景回归 |
| 地点 | 六钓场识别和策略选择，码头/选岛/启航与恢复返回 | 开始页入口、天空岛专属机制、稳定身份和完整玩法信息读取 |
| QTE | 黄区优先、仅蓝区内部边界与连续确认、明暗指针区分、同次入区去重 | 高速/遮挡/窄区与跨电脑实际命中率 |
| 特殊机制 | 局部红紫/贝壳避让、多泡泡去重、残影局部排除、绿色范围外普通目标与多外观时间线 | 冰冻专用识别与解除、绿色可靠起按端、多墙、隐形/特效指针及组合技能因果 |
| 维护取证 | 失败、异常、未确认与特殊外观常驻取证；后台有界写盘 | 稀疏证据不能解释所有历史 unknown，也不能证明技能解除 |
| 设置与时延 | 配置导入和校验、客户区检测、手动参数、基础等待校准 | 游戏端到端时延基线与按真实命中率自动调参 |
| 更新 | 在线正式版、本地 ZIP、逐文件校验、备份恢复与数据保留 | 无增量包、拖放、降级、静默安装或数字签名 |

绿色控制器已有状态与模拟回归，但真实起按端未确认，实际识别不自动授权长按。泡泡发送一次按键只算尝试；红紫颜色也不等于已确认技能。完整机制链尚未贯通，详见[审计](mechanism-chain-audit.md)。

## 已验证范围

| 检查 | 结果与边界 |
| --- | --- |
| 本机离线回归 | 2026-09-14 当前工作区 574 项通过，含真实截图规则与模拟设备/资源测试；仍存在审查发现的覆盖缺口 |
| 规范和界面 | 2026-09-14 Ruff、112 模块架构、隐藏 Tk 主窗口与更新窗口检查通过，不等于可视布局全覆盖 |
| 本机候选更新助手 | 隔离目录中验证等待退出、替换、旧依赖清理、配置保留、重启及中断恢复，未操作游戏 |
| 官方附件 | 使用程序下载与解包模块完成公开 ZIP 下载、整体 SHA-256 与全清单校验 |
| 正式版查询 | 初次匿名查询遇到 HTTP 403 限流；解除后复核 0.1.0 可发现 0.2.0，0.2.0 返回无更新 |
| 发布后回归稳定性 | 修正 Windows 路径别名及轮转测试的同时间戳样本假设；最终发布/文档 PR 的 Windows CI 通过 |

本轮审查证据位于 `.local/maintenance/code-review-20260914/`；上表更新助手、官方附件和查询条目是 2026-09-10 发布阶段的历史验证，证据位于 `.local/maintenance/portable-update-20260910/` 和 `.local/maintenance/publish-20260910/`，本轮没有重新执行。证据不随 GitHub 源码提供，历史普通钓鱼成功不能替代新版所有机制和设备组合的实机验收。

## 下一步

1. 以明确版本与哈希开展限时游戏验收，记录配置、客户区、DPI、输入及反馈，不同时调整多个时序参数。
2. 优先复核仅蓝区、泡泡/绿色、遮挡与多指针等已有样本；新机制补全正例、负例和连续前后文。
3. 对新 unknown 先区分采集异常、文字漏检与输入归属歧义，保留原始结果，不用异常包数量推算命中率。
4. 根据实际游戏画面补岛屿资料、导航和信息读取，继续按功能边界扩展，不预建空框架。

每项完成判据见[验证计划](validation-plan.md)。
