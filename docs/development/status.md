# 当前开发状态

[文档目录](../README.md) · [历史实测档案](../history/optimization-2026-09.md)

本页汇总截至 2026-09-09 的源码状态。当前目录以[统一布局](../design/repository-layout.md)为准；结构调整历史保留当时路径，相关旧 debug 记录已归入本机 archive/development/2026-09-08/debug。历史档案中的“默认关闭”“未合并”和旧发布状态只对应当时记录。

## 最新：分身识别与特殊场景取证（仅离线验证）

按用户看图确认的规则，两套 QTE 策略共用窄竖线候选与亮度比较，以最亮可信光标判断目标区重合。
暗候选、亮度歧义或形状不足时不猜测输入。F05/F06 清晰观察原图与颜色/粒子/边框负例回归通过；
旧决策图与另一时刻的观察图分开使用。U07 原图序列验证蓝区外不按、进入蓝区后回退输入一次，
不把原始 unknown 改为成功。

正式反馈观察器接入特殊场景候选和整轮低频时间线，不依赖按键、失败或调试开关；
成功捕获也保留特殊候选。复用已有 GDI 原图，事件与缓存有界，PNG/ZIP 写入使用后台队列。
没有新增输入排队或逐帧等待，仅在整轮关闭时对场景写入最多等待一秒。
路径、上限与遗漏边界见[维护取证](../user/diagnostics.md)。

185 项离线回归、Ruff 检查、123 个文件格式检查及 67 模块架构检查通过。
固定图册 60 包的 480 张原图回放产生 33 份候选包、27 份普通包，244 张选中图像全部解码通过，
本次缓存丢帧为 0。普通背景光晕未误触发紫/绿色候选，另用既有紫色图示和红色牙齿原图验证外观取证。
本机 945×532 样本的指针定位 p95 为 0.607ms、场景观察计算 p95 为 0.712ms；
这是局部离线计算时间，不含截图驱动、游戏输入和真实反馈，不是端到端时延保证。
报告位于 `.local/maintenance/offline-scene-replay-20260909/run-082023/summary.json`。

F02–F04 的历史序列中找到过蓝区可按窗口，F01 现有短片未覆盖可命中瞬间，不能证明其具体原因已解决。
真正冰冻的识别/破冰/解除确认、绿色维持、隐形与交叠跟踪等仍有缺口，见[特殊机制处理](../design/special-mechanism-handling.md)。
本轮未操作游戏、构建或更新部署 EXE。

## 已有能力与边界

| 范围 | 当前状态 |
| --- | --- |
| 页面与启停 | 默认待机，按钮控制单个任务；重新启动重建所需状态；停止释放输入与截图 |
| 前台与显示器 | 聚焦失败不运行；失焦、锁屏和窗口几何变化停止；支持单屏内负坐标，不支持跨屏拼接 |
| 抛竿与满包 | 位置受阻先恢复重抛；同轮仍失败才停止；自动清包开关生效 |
| 等待上钩 | 夜间黄色 H 上限 35，已有真实正负例；遮挡、小窗口与其他环境仍需按原图定位 |
| QTE 策略 | 黄/蓝区、挡板；无光标不盲按、同次重合去重、普通策略有蓝无黄回退及红色遮挡避让。已移除未经验证的红色即冰冻按键；真正冰冻恢复等机制仍待补全，见 error-triage |
| 反馈与证据 | 默认开启；游戏反馈、按键尝试和归属未确认分开记录；失败包可附控制决策原图，特殊候选与整轮低频时间线独立保存 |
| 整条鱼结果 | 奖励证据充分才确认捕获；疑似超时逃脱保留推断属性，中断单独记录 |
| 日志 | 文件保留细节，页面筛选只影响展示副本；轮次与证据 ID 用于关联记录 |

特殊机制逐项覆盖见[机制参考](../reference/fishing-mechanics.md)。亮指针选择已有离线回归；绿色维持、隐形/交叠及其他机制不能因已经记录到反馈就认定策略已支持。

历史已测试功能与当前正式入口的逐项核对见[已验证功能清单](verified-features.md)。旧实验分支与旧 main 已在 2233919 汇合，后续重构保留了该版本的反馈、结算、停止与采集能力；不需要将旧目录再引入当前仓库。

## 验证与发布边界

历史实测包括本机 875×492 客户区、真实反馈与部分确认捕获样本，也记录过未捕获和未知结果；不能据此推算百分之百命中率。其他分辨率、语言和机制叠加需另行验证。

GitHub 源码、构建产物和用户电脑已有 EXE 分别管理。源码更新不自动覆盖已有 EXE；发布版功能以其构建提交和验收记录为准。

## 暂缓与后续线索

- 继续补全尚未识别的特殊机制；已接入的外观候选与低频时间线不能保证捕获任意短暂机制。
- 装扮遮挡、隐形/交叠指针、绿色维持等按实际原图逐项建立回归，不单凭日志下调阈值。
- 多分辨率、混合 DPI、显示器唤醒后重新启动等需要有条件的实机验证，不主动锁屏或让电脑睡眠。

## 当前：结构规范与低时延边界

[开发规范](standards.md)定义已经执行的依赖方向、公共逻辑归属、Ruff 与 CI 检查，区别于完整设备注入等后续架构目标。

- runtime 仅保留通用运行能力；通用图像、OCR 类型/读取、文字处理与识别上下文归 perception；游戏观察区域、反馈字形和 QTE 统计归 game。infrastructure 不再反向导入钓鱼图像模块，结算不再从反馈会话借用识别函数。
- Ruff 固定为开发依赖，统一源码、脚本与测试的格式和基础静态检查。修复只读截图检查中 window 模块被局部变量遮蔽的问题，增加不连接游戏的入口回归。
- 静态依赖检查覆盖 61 个模块、导入方向和循环；另有违规别名/相对循环样本验证检查器。CI 已配置 push/PR 的格式、静态检查、架构检查、离线回归与 wheel 构建，本地检查已通过，远程结果以 GitHub Actions 为准。
- 文件日志改为有界后台输出，详细日志不能占用为结果/错误预留的容量；慢输出、队列过载、轮次/异常保持与关闭排空均有回归。队列耗尽或异常退出仍可能丢记录，后台明确报告，不宣称无损日志。
- 134 项离线回归通过，Tk 模拟检查通过。三个 QTE 策略类 AST 与本轮备份一致，等待参数、驱动暂停、按键条件及顺序未改动。
- 普通 wheel 与 PyInstaller 候选包已构建，核对源码模块、模板、默认 INI、OCR 模型和 Tk 资源；本机部署维持现有版本。

离线基准使用固定种子合成图像及真实临时文件日志，每项 2000 个计时样本。它只测局部工作量，不包含截图驱动、游戏输入、等待或真实反馈：

| 范围 | p50 (ms) | p95 (ms) | p99 (ms) |
| --- | --- | --- | --- |
| 400×80 图像原语 | 0.2571 | 0.4208 | 0.6066 |
| 800×160 图像原语 | 0.7892 | 1.3279 | 1.5848 |
| 同步文件处理器 | 0.0121 | 0.0230 | 0.0404 |
| 后台文件日志提交 | 0.0044 | 0.0096 | 0.0154 |

本次基准日志丢弃数为 0；这是本机短时样本，不能当作其他机器的性能保证。后台锁、OCR 争用、驱动开销和游戏端到端响应仍需实测。

本轮记录位于 `.local/maintenance/standards-*.log`，基准结果位于 `.local/benchmarks/qte-latency.json`，调整前源码保存在 `.local/maintenance/before-standards-audit.zip`。

本轮未操控游戏或升级本机部署。全工作区迁移和前几轮结构调整记录见[结构调整历史](../history/structure-2026-09.md)。

## 构建元数据位置修正

setuptools 默认将 egg-info 放在 src 顶层，本机此前也采用此默认。现由最小 setup.py 将元数据与中间文件固定到 build，pyproject 继续作为项目元数据和依赖的唯一来源。原 bd2_fishing.egg-info 已移入本机维护备份，该阶段 src 顶层只保留源码包；本轮进一步移除了 src 层，现以目录说明为准。

可编辑安装、wheel 构建、没有 .local 的全新副本、sdist 及从 sdist 再构建 wheel 均通过检查；默认配置与六张模板完整。新增离线包装回归检查生成位置。日志见 .local/maintenance/metadata-*.log；业务代码和部署未改动。

## GitHub 源码版本检查

本次提交汇总目录归类、src 职责拆分、公共识别模块、后台日志、开发检查与构建路径修正。推送前 Ruff 检查、108 个 Python 文件格式检查、61 个模块架构检查和 135 项离线回归通过。树形图与目录职责见[统一布局](../design/repository-layout.md)。本机 deployment、archive、.venv、.local 和个人配置不属于 GitHub 源码版本。


## 独立应用目录与正常运行取证

应用源码从 src/bd2_fishing 移至根目录 bd2_fishing；构建中间物迁到 build，便携候选包迁到 dist；deployment/current 合并为 deployment，1177 个部署文件 SHA256 前后一致，未替换旧 EXE。配置和诊断仍留在 .local，个人配置不覆盖。

QTE/结算观察不再受旧调试开关控制。普通任务异常、恢复前现场、失败和未确认结果保存维护证据；失败和成功分目录，时间与唯一 ID 命名，跨重启按数量和容量清理。源码和新候选包统一使用 diagnostics 名称，旧部署 debug 不迁移。

新增关闭调试后的记录、后台错误、缓存所有权、无图说明、跨重启保留、磁盘慢写、失焦保护和成功不覆盖失败的回归。143 项离线测试及 63 模块架构检查通过。模拟 Tk 界面、wheel 和便携包构建通过；核对 63 个模块、6 张识别模板、默认配置、3 个 OCR 模型、Tcl/Tk 和 ZIP 完整性。真实游戏、未知岛屿机制和端到端时延尚未实测；候选构建不代表部署已更新。

## 提交前功能合入与架构复核

147 项离线测试通过。通用证据写入器改为 bundle_writer，异常堆栈格式化移入后台，上钩超时复用已有现场图。静态检查补充禁止正式应用导入实验脚本或测试代码；两套 QTE 策略和历史验证的关键规则保持一致。

重新执行历史结算原图的 OCR：两份捕获仍为 28.6cm、21.7cm，疑似逃脱仍为剩余 1980cm；使用正式结算入口保存复核记录。30 个历史 QTE ZIP 的 240 张原图与 720 张掩膜完整且一致。限时停止后的零输入结论来自既有实测，本轮未重新操控游戏。详细功能映射与验证边界见 verified-features。

## 审查修复验证

2026-09-08 修复四项审查发现：按键观察改为容量 128 的非阻塞提交，反馈日志移出状态锁；关闭先封存反馈与 OCR 读数，拒绝迟到结果；换岛确认超时取证并停止，不继续抛竿；三类证据共用写入事务，失败清理本次临时文件并保留此前完整 ZIP。

新增 12 项离线回归，覆盖慢日志与输入取消、队列过载、迟到识别、OCR 取消、关闭后提交、换岛成功/超时/取消及失败写入恢复。全部 159 项离线测试通过；63 模块依赖检查、Ruff 和格式检查通过。日志位于 .local/maintenance/fix-review-tests.log。

上述离线修复阶段未操控游戏，未重新构建候选包或覆盖部署。原生识别无法强制中断时由原线程返回后释放资源，封存状态拒绝迟到结果。

## 修复后的实机验证与提交依据

2026-09-08 经用户授权，在亚特兰蒂斯、945×532 客户区执行两段各约 80 秒的真实源码测试。沿用正式 TaskController、run_once、DXcam/GDI 和受控输入，记录实际 SendInput、按键观察提交耗时与整轮结果；临时关闭自动清包和自动换岛，不改写个人配置。

首段实际触发并完成位置受阻恢复，记录一次未确认、一次确认捕获（5.2cm）和一次主动中断。核对未确认原图发现关闭提示误判：灰字模板缩放后被 V≥185 阈值全部滤掉，使常量模板的归一化相关分数恒为 1。改为灰度字形、±1 像素取整搜索及常量模板保护，保持匹配门槛 0.85；新增本次正负原图、常量输入和无效模板回归。

第二段使用最终修正源码，结果如下：

| 项目 | 实测结果 |
| --- | --- |
| 整轮结果 | 确认捕获 5.5cm、3.3cm；第三轮在 QTE 中主动停止并记录 interrupted |
| 按键与取证 | 55 次 QTE 按键观察提交；丢失记录 0；141 次系统输入调用全部接受 |
| 停止 | 停止后非释放类输入 0；工作线程约 78ms 后退出；任务/QTE 观察与写入线程未残留 |
| 输入释放 | space、up、t、鼠标左键均未保持按下；重复开始未创建第二工作线程 |
| 提交局部耗时 | begin_press：p50 0.023ms、p95 0.0321ms、p99 0.0602ms、max 0.0901ms，55 样本 |
| 证据 | 36 ZIP、304 原图、891 掩膜；ZIP 校验、图片解码及同帧 HSV 掩膜逐一通过；临时文件 0 |

两段合计 67 ZIP、553 原图、1608 掩膜均通过校验。原始日志、输入事件、轮次与摘要位于本机 .local/maintenance/live-review-20260908-214612 和 live-review-20260908-215302，证据核对汇总为 live-review-evidence-audit.json。代表性正负原图已纳入 tests/fixtures/catch_result，运行记录其余部分不提交。

最终 162 项离线测试、Ruff、格式和 63 模块架构检查通过。换岛超时、磁盘故障及慢日志/迟到线程边界仍由离线故障注入验证，未在游戏里制造这些故障。局部提交耗时不代表游戏端到端响应，也不承诺所有岛屿和特殊机制已覆盖。本次提交源码，不打包、不替换部署 EXE。

## 实机错误分类与逐项修复

2026-09-08 将真实故障、机制覆盖缺口、反馈未确认和主动中断分开，逐项证据及状态见[问题清单](error-triage.md)。正式代码移除无光标盲按，两套 QTE 共用进入目标区的触发状态；普通策略连续两次有蓝无黄时允许蓝区回退。新实测明确发现红色牙齿图示误触发旧破冰规则，移除“红像素即冰冻”的盲按，改为避开光标附近红色遮挡。真正冰冻恢复仍待真实正例与专门流程，不能称为已支持。

新增 10 项离线回归、9 份真实原图；172 项测试、Ruff、118 文件格式及 64 模块架构检查通过。两段阶段实测确认捕获三条鱼，其中一段仍记录未确认整轮并暴露红色误触发。最终 22:27:53 的 80 秒实测确认捕获狮子鱼 32cm、31.8cm，25 次暴击、0 次 MISS/FAIL，第三轮主动停止；27 次按键中两次 unknown 的决策帧带紫色图示，不强行归属。其中蓝区回退一次尚无可确认命中反馈，红色遮挡本轮未复现，其防护由真实负例回归验证。

最终停止后无操作输入、按键保持或相关线程残留；工作线程约 94ms 退出，取证提交 p95 0.0300ms。5 ZIP、25 原图、54 掩膜校验通过。没有改动个人配置、输入驱动暂停、循环等待或已部署 EXE。特殊机制不能因本次两条鱼捕获成功就视为全部验收。
